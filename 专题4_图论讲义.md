# 专题4: 图论讲义 🕸️
## 初一学生专用版 - 连接世界的网络！

## 课程信息
- **授课老师**: 刘朔 (清华姚班)
- **课程时间**: 10月5日 08:00-11:30
- **目标学生**: 初一学生，准备冲刺CSP-J高分
- **课程性质**: B层训练营核心课程

---

## 目录
1. [图论是什么？](#1-图论是什么)
2. [图的基本概念 - 点和线的世界](#2-图的基本概念---点和线的世界)
3. [图的表示方法 - 如何画图](#3-图的表示方法---如何画图)
4. [图的遍历 - 探索网络](#4-图的遍历---探索网络)
5. [最短路径 - 找最近的路](#5-最短路径---找最近的路)
6. [最小生成树 - 连接所有点](#6-最小生成树---连接所有点)
7. [拓扑排序 - 安排顺序](#7-拓扑排序---安排顺序)
8. [趣味例题](#8-趣味例题)
9. [挑战练习](#9-挑战练习)

---

## 1. 图论是什么？

### 1.1 图论就像社交网络 🌐
**核心原理**: 图论就是研究点和线连接关系的学问，就像研究社交网络中人与人之间的关系！

**简单理解**: 图论 = 点 + 线 + 关系

**生活例子**: 就像微信朋友圈，每个人是一个点，朋友关系是线，整个朋友圈就是一个图。

### 1.2 图论的应用场景 🎯
- **社交网络**: 朋友关系、关注关系
- **交通网络**: 城市之间的道路连接
- **计算机网络**: 服务器之间的连接
- **游戏地图**: 游戏中的路径和关卡

**记忆口诀**: "社交交通网络游戏，图论无处不在！"

### 1.3 图论的重要性 ⭐
- **连接世界**: 帮助我们理解复杂的关系
- **优化路径**: 找到最短或最优的路径
- **分析网络**: 理解网络的结构和特性
- **解决实际问题**: 从导航到社交推荐

**生活例子**: 就像GPS导航，需要找到从家到学校的最短路径。

---

## 2. 图的基本概念 - 点和线的世界 🔗

### 2.1 图的基本组成 🧩
- **顶点(Vertex)**: 图中的点，也叫节点
- **边(Edge)**: 连接两个顶点的线
- **图(Graph)**: 由顶点和边组成的结构

**生活例子**: 就像班级关系图，每个同学是一个顶点，朋友关系是边。

### 2.2 图的分类 📊

#### 2.2.1 有向图和无向图
- **无向图**: 边没有方向，A和B是朋友，B和A也是朋友
- **有向图**: 边有方向，A关注B，但B不一定关注A

```cpp
// 无向图：朋友关系
A ---- B
|      |
C ---- D

// 有向图：关注关系
A --> B
^     |
|     v
C <-- D
```

#### 2.2.2 有权图和无权图
- **无权图**: 边没有权重，只表示连接关系
- **有权图**: 边有权重，表示距离、时间、成本等

```cpp
// 无权图：朋友关系
A ---- B
|      |
C ---- D

// 有权图：距离关系
A --3-- B
|       |
2       4
|       |
C --1-- D
```

### 2.3 图的基本术语 📚
- **度(Degree)**: 与顶点相连的边的数量
- **路径(Path)**: 从一个顶点到另一个顶点的边的序列
- **环(Cycle)**: 起点和终点相同的路径
- **连通图**: 任意两个顶点之间都有路径
- **强连通图**: 有向图中任意两个顶点都互相可达

**生活例子**: 
- 度：一个人的朋友数量
- 路径：从A到B需要经过哪些朋友
- 环：A认识B，B认识C，C认识A
- 连通：班级里任意两个同学都能通过朋友关系联系上

**图的基本概念记忆口诀**: "顶点边组成图，有向无向有权重，度路径环连通！"

---

## 3. 图的表示方法 - 如何画图 🎨

### 3.1 邻接矩阵 - 表格表示法 📋
**核心原理**: 用二维数组表示图，行和列都代表顶点，数组值表示是否有边！

**简单理解**: 邻接矩阵 = 表格 + 0和1

**生活例子**: 就像班级座位表，行代表一个同学，列代表另一个同学，1表示是朋友，0表示不是朋友。

```cpp
// 无向图的邻接矩阵
int graph[4][4] = {
    {0, 1, 1, 0},  // A: 与B、C相连
    {1, 0, 0, 1},  // B: 与A、D相连
    {1, 0, 0, 1},  // C: 与A、D相连
    {0, 1, 1, 0}   // D: 与B、C相连
};

// 有权图的邻接矩阵
int weightedGraph[4][4] = {
    {0, 3, 2, 0},  // A: 到B距离3，到C距离2
    {3, 0, 0, 4},  // B: 到A距离3，到D距离4
    {2, 0, 0, 1},  // C: 到A距离2，到D距离1
    {0, 4, 1, 0}   // D: 到B距离4，到C距离1
};
```

### 3.2 邻接表 - 列表表示法 📝
**核心原理**: 用链表数组表示图，每个顶点对应一个链表，存储与它相连的顶点！

**简单理解**: 邻接表 = 数组 + 链表

**生活例子**: 就像每个人的朋友列表，每个人有一个列表记录所有朋友。

```cpp
#include <vector>

// 邻接表表示
vector<vector<int>> adjList(4);  // 4个顶点

// 添加边
void addEdge(int from, int to) {
    adjList[from].push_back(to);
    adjList[to].push_back(from);  // 无向图需要双向添加
}

// 初始化图
adjList[0] = {1, 2};  // A连接B、C
adjList[1] = {0, 3};  // B连接A、D
adjList[2] = {0, 3};  // C连接A、D
adjList[3] = {1, 2};  // D连接B、C

// 带权重的邻接表
struct Edge {
    int to;
    int weight;
    Edge(int t, int w) : to(t), weight(w) {}
};

vector<vector<Edge>> weightedAdjList(4);
weightedAdjList[0] = {Edge(1, 3), Edge(2, 2)};
weightedAdjList[1] = {Edge(0, 3), Edge(3, 4)};
weightedAdjList[2] = {Edge(0, 2), Edge(3, 1)};
weightedAdjList[3] = {Edge(1, 4), Edge(2, 1)};
```

### 3.3 两种表示方法的比较 ⚖️
- **邻接矩阵**:
  - 优点：查找边很快，适合稠密图
  - 缺点：空间占用大，不适合稀疏图
- **邻接表**:
  - 优点：空间占用小，适合稀疏图
  - 缺点：查找边较慢，需要遍历链表

**选择原则**: 稠密图用邻接矩阵，稀疏图用邻接表

**图的表示方法记忆口诀**: "邻接矩阵表格法，邻接表链表法，稠密用矩阵，稀疏用链表！"

---

## 4. 图的遍历 - 探索网络 🗺️

### 4.1 深度优先搜索(DFS) - 一条路走到黑 🚶‍♂️
**核心原理**: 就像走迷宫，选择一条路一直走下去，直到走不通再回头换另一条路！

**简单理解**: DFS = 一条路走到黑 + 回溯

**生活例子**: 就像探索新城市，选择一条街道一直走，走到底再回来探索其他街道。

```cpp
// DFS递归实现
vector<bool> visited(4, false);

void dfs(int vertex) {
    visited[vertex] = true;  // 标记已访问
    cout << vertex << " ";   // 访问当前顶点
    
    // 遍历所有邻居
    for (int neighbor : adjList[vertex]) {
        if (!visited[neighbor]) {
            dfs(neighbor);  // 递归访问
        }
    }
}

// DFS栈实现
void dfsStack(int start) {
    vector<bool> visited(4, false);
    stack<int> stk;
    
    stk.push(start);
    visited[start] = true;
    
    while (!stk.empty()) {
        int current = stk.top();
        stk.pop();
        
        cout << current << " ";
        
        // 将未访问的邻居入栈
        for (int neighbor : adjList[current]) {
            if (!visited[neighbor]) {
                visited[neighbor] = true;
                stk.push(neighbor);
            }
        }
    }
}
```

### 4.2 广度优先搜索(BFS) - 齐头并进 🌊
**核心原理**: 就像水波扩散，从起点开始，同时向四面八方探索，一层一层地推进！

**简单理解**: BFS = 齐头并进 + 层次遍历

**生活例子**: 就像石头扔进水里，水波一圈一圈地扩散，同时到达所有方向。

```cpp
void bfs(int start) {
    vector<bool> visited(4, false);
    queue<int> q;
    
    q.push(start);
    visited[start] = true;
    
    while (!q.empty()) {
        int current = q.front();
        q.pop();
        
        cout << current << " ";
        
        // 将未访问的邻居入队
        for (int neighbor : adjList[current]) {
            if (!visited[neighbor]) {
                visited[neighbor] = true;
                q.push(neighbor);
            }
        }
    }
}

// 带距离的BFS
vector<int> bfsWithDistance(int start) {
    vector<bool> visited(4, false);
    vector<int> distance(4, -1);
    queue<int> q;
    
    q.push(start);
    visited[start] = true;
    distance[start] = 0;
    
    while (!q.empty()) {
        int current = q.front();
        q.pop();
        
        for (int neighbor : adjList[current]) {
            if (!visited[neighbor]) {
                visited[neighbor] = true;
                distance[neighbor] = distance[current] + 1;
                q.push(neighbor);
            }
        }
    }
    
    return distance;
}
```

### 4.3 遍历的应用 🎯
- **连通性检测**: 判断图是否连通
- **路径查找**: 找到两个顶点之间的路径
- **环检测**: 检测图中是否存在环
- **拓扑排序**: 对有向无环图进行排序

**图的遍历记忆口诀**: "DFS一条路走到黑，BFS齐头并进，遍历探索网络！"

---

## 5. 最短路径 - 找最近的路 🛣️

### 5.1 单源最短路径 - 从一个点到所有点 📍
**核心原理**: 找到从一个起点到图中所有其他顶点的最短路径！

**简单理解**: 单源最短路径 = 一个起点 + 到所有点的最短距离

**生活例子**: 就像从家出发，找到到学校、商场、公园等所有地方的最短路径。

### 5.2 Dijkstra算法 - 贪心找最短 🎯
**核心原理**: 每次选择距离起点最近的未访问顶点，更新其邻居的距离！

**简单理解**: Dijkstra = 贪心 + 距离更新

**生活例子**: 就像GPS导航，总是选择当前距离最短的路继续走。

```cpp
#include <queue>
#include <climits>

vector<int> dijkstra(int start, vector<vector<pair<int, int>>>& graph) {
    int n = graph.size();
    vector<int> distance(n, INT_MAX);
    vector<bool> visited(n, false);
    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> pq;
    
    distance[start] = 0;
    pq.push({0, start});
    
    while (!pq.empty()) {
        int current = pq.top().second;
        pq.pop();
        
        if (visited[current]) continue;
        visited[current] = true;
        
        for (auto& edge : graph[current]) {
            int neighbor = edge.first;
            int weight = edge.second;
            
            if (distance[current] + weight < distance[neighbor]) {
                distance[neighbor] = distance[current] + weight;
                pq.push({distance[neighbor], neighbor});
            }
        }
    }
    
    return distance;
}
```

### 5.3 Floyd-Warshall算法 - 所有点对最短路径 🌐
**核心原理**: 通过中间顶点逐步更新所有点对之间的最短距离！

**简单理解**: Floyd-Warshall = 动态规划 + 所有点对

**生活例子**: 就像制作一个完整的距离表，记录任意两个城市之间的最短距离。

```cpp
vector<vector<int>> floydWarshall(vector<vector<int>>& graph) {
    int n = graph.size();
    vector<vector<int>> distance = graph;
    
    // 初始化距离矩阵
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            if (i != j && distance[i][j] == 0) {
                distance[i][j] = INT_MAX;  // 没有直接连接
            }
        }
    }
    
    // 通过中间顶点k更新距离
    for (int k = 0; k < n; k++) {
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                if (distance[i][k] != INT_MAX && distance[k][j] != INT_MAX) {
                    distance[i][j] = min(distance[i][j], distance[i][k] + distance[k][j]);
                }
            }
        }
    }
    
    return distance;
}
```

### 5.4 最短路径的应用 🎯
- **导航系统**: GPS路径规划
- **网络路由**: 数据包传输路径
- **游戏AI**: 角色移动路径
- **物流优化**: 配送路径规划

**最短路径记忆口诀**: "Dijkstra贪心单源，Floyd动态全对，最短路径导航用！"

---

## 6. 最小生成树 - 连接所有点 🌳

### 6.1 最小生成树的概念 🌲
**核心原理**: 用最少的边连接图中的所有顶点，且总权重最小！

**简单理解**: 最小生成树 = 连接所有点 + 权重最小

**生活例子**: 就像给一个小区铺设水管，要连接所有房子，但总成本要最小。

### 6.2 Kruskal算法 - 贪心选边 🎯
**核心原理**: 按权重从小到大选择边，如果不会形成环就加入生成树！

**简单理解**: Kruskal = 贪心选边 + 环检测

**生活例子**: 就像修路，总是先修最便宜的路，但不能形成环路。

```cpp
#include <algorithm>

struct Edge {
    int from, to, weight;
    Edge(int f, int t, int w) : from(f), to(t), weight(w) {}
};

class UnionFind {
private:
    vector<int> parent;
    
public:
    UnionFind(int n) : parent(n) {
        for (int i = 0; i < n; i++) {
            parent[i] = i;
        }
    }
    
    int find(int x) {
        if (parent[x] != x) {
            parent[x] = find(parent[x]);  // 路径压缩
        }
        return parent[x];
    }
    
    bool unite(int x, int y) {
        int px = find(x);
        int py = find(y);
        if (px == py) return false;  // 已经在同一集合
        
        parent[px] = py;
        return true;
    }
};

vector<Edge> kruskal(vector<Edge>& edges, int n) {
    sort(edges.begin(), edges.end(), [](const Edge& a, const Edge& b) {
        return a.weight < b.weight;  // 按权重排序
    });
    
    UnionFind uf(n);
    vector<Edge> mst;
    
    for (const Edge& edge : edges) {
        if (uf.unite(edge.from, edge.to)) {  // 不会形成环
            mst.push_back(edge);
            if (mst.size() == n - 1) break;  // 生成树有n-1条边
        }
    }
    
    return mst;
}
```

### 6.3 Prim算法 - 贪心选点 🌱
**核心原理**: 从任意顶点开始，每次选择距离已选顶点集合最近的顶点！

**简单理解**: Prim = 贪心选点 + 距离更新

**生活例子**: 就像种树，从一棵树开始，每次选择距离已种树最近的地方种新树。

```cpp
vector<Edge> prim(vector<vector<pair<int, int>>>& graph) {
    int n = graph.size();
    vector<bool> inMST(n, false);
    vector<int> key(n, INT_MAX);
    vector<int> parent(n, -1);
    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> pq;
    
    key[0] = 0;
    pq.push({0, 0});
    
    while (!pq.empty()) {
        int u = pq.top().second;
        pq.pop();
        
        if (inMST[u]) continue;
        inMST[u] = true;
        
        for (auto& edge : graph[u]) {
            int v = edge.first;
            int weight = edge.second;
            
            if (!inMST[v] && weight < key[v]) {
                key[v] = weight;
                parent[v] = u;
                pq.push({key[v], v});
            }
        }
    }
    
    vector<Edge> mst;
    for (int i = 1; i < n; i++) {
        mst.push_back(Edge(parent[i], i, key[i]));
    }
    
    return mst;
}
```

### 6.4 最小生成树的应用 🎯
- **网络设计**: 设计通信网络
- **电路设计**: 连接电路元件
- **城市规划**: 设计道路网络
- **聚类分析**: 数据聚类

**最小生成树记忆口诀**: "Kruskal贪心选边，Prim贪心选点，连接所有点权重最小！"

---

## 7. 拓扑排序 - 安排顺序 📋

### 7.1 拓扑排序的概念 📚
**核心原理**: 对有向无环图进行排序，使得对于每条边(u,v)，u在排序中出现在v之前！

**简单理解**: 拓扑排序 = 有向图 + 顺序安排

**生活例子**: 就像安排课程表，有些课程必须先学，有些课程可以后学。

### 7.2 Kahn算法 - 入度统计法 📊
**核心原理**: 不断删除入度为0的顶点，直到所有顶点都被删除！

**简单理解**: Kahn = 入度统计 + 逐步删除

**生活例子**: 就像选课，先选没有先修要求的课程，选完后其他课程的先修要求就满足了。

```cpp
vector<int> topologicalSort(vector<vector<int>>& graph) {
    int n = graph.size();
    vector<int> inDegree(n, 0);
    vector<int> result;
    
    // 计算入度
    for (int i = 0; i < n; i++) {
        for (int neighbor : graph[i]) {
            inDegree[neighbor]++;
        }
    }
    
    queue<int> q;
    
    // 将入度为0的顶点入队
    for (int i = 0; i < n; i++) {
        if (inDegree[i] == 0) {
            q.push(i);
        }
    }
    
    while (!q.empty()) {
        int current = q.front();
        q.pop();
        result.push_back(current);
        
        // 更新邻居的入度
        for (int neighbor : graph[current]) {
            inDegree[neighbor]--;
            if (inDegree[neighbor] == 0) {
                q.push(neighbor);
            }
        }
    }
    
    // 如果结果数量不等于顶点数量，说明有环
    if (result.size() != n) {
        return {};  // 有环，无法拓扑排序
    }
    
    return result;
}
```

### 7.3 DFS拓扑排序 - 深度优先法 🔍
**核心原理**: 使用DFS遍历图，在回溯时将顶点加入结果！

**简单理解**: DFS拓扑排序 = 深度优先 + 回溯记录

```cpp
vector<int> topologicalSortDFS(vector<vector<int>>& graph) {
    int n = graph.size();
    vector<int> color(n, 0);  // 0: 白色(未访问), 1: 灰色(访问中), 2: 黑色(已访问)
    vector<int> result;
    
    function<bool(int)> dfs = [&](int u) -> bool {
        color[u] = 1;  // 标记为访问中
        
        for (int v : graph[u]) {
            if (color[v] == 1) return false;  // 发现环
            if (color[v] == 0 && !dfs(v)) return false;
        }
        
        color[u] = 2;  // 标记为已访问
        result.push_back(u);  // 在回溯时加入结果
        return true;
    };
    
    for (int i = 0; i < n; i++) {
        if (color[i] == 0) {
            if (!dfs(i)) {
                return {};  // 有环，无法拓扑排序
            }
        }
    }
    
    reverse(result.begin(), result.end());  // 反转结果
    return result;
}
```

### 7.4 拓扑排序的应用 🎯
- **课程安排**: 安排有先修要求的课程
- **任务调度**: 安排有依赖关系的任务
- **编译顺序**: 确定源代码文件的编译顺序
- **依赖管理**: 管理软件包的依赖关系

**拓扑排序记忆口诀**: "Kahn入度统计法，DFS深度优先法，有向无环图排序！"

---

## 8. 趣味例题 🎮

### 例题1: 课程表 - 选课游戏 📚
**题目**: 给定课程数量和先修关系，判断是否能完成所有课程。

**生活例子**: 就像选课，有些课程需要先学其他课程，问能否安排出合理的选课顺序。

```cpp
bool canFinish(int numCourses, vector<vector<int>>& prerequisites) {
    vector<vector<int>> graph(numCourses);
    vector<int> inDegree(numCourses, 0);
    
    // 构建图和入度
    for (auto& pre : prerequisites) {
        graph[pre[1]].push_back(pre[0]);  // pre[1] -> pre[0]
        inDegree[pre[0]]++;
    }
    
    queue<int> q;
    
    // 将入度为0的课程入队
    for (int i = 0; i < numCourses; i++) {
        if (inDegree[i] == 0) {
            q.push(i);
        }
    }
    
    int completed = 0;
    
    while (!q.empty()) {
        int course = q.front();
        q.pop();
        completed++;
        
        // 更新依赖课程的入度
        for (int nextCourse : graph[course]) {
            inDegree[nextCourse]--;
            if (inDegree[nextCourse] == 0) {
                q.push(nextCourse);
            }
        }
    }
    
    return completed == numCourses;  // 所有课程都能完成
}
```

### 例题2: 岛屿数量 - 数岛屿游戏 🏝️
**题目**: 给定二维网格，计算岛屿的数量。

**生活例子**: 就像在地图上数有多少个岛屿，相邻的陆地算作一个岛屿。

```cpp
void dfsIsland(vector<vector<char>>& grid, int x, int y) {
    if (x < 0 || x >= grid.size() || y < 0 || y >= grid[0].size() 
        || grid[x][y] != '1') {
        return;  // 越界或不是陆地
    }
    
    grid[x][y] = '0';  // 标记为已访问
    
    // 四个方向递归
    dfsIsland(grid, x + 1, y);
    dfsIsland(grid, x - 1, y);
    dfsIsland(grid, x, y + 1);
    dfsIsland(grid, x, y - 1);
}

int numIslands(vector<vector<char>>& grid) {
    int count = 0;
    
    for (int i = 0; i < grid.size(); i++) {
        for (int j = 0; j < grid[0].size(); j++) {
            if (grid[i][j] == '1') {  // 发现新岛屿
                dfsIsland(grid, i, j);  // 标记整个岛屿
                count++;
            }
        }
    }
    
    return count;
}
```

### 例题3: 单词接龙 - 接龙游戏 🔗
**题目**: 给定两个单词和一个单词列表，找到从起始单词到结束单词的最短转换序列。

**生活例子**: 就像单词接龙游戏，每次只能改变一个字母，要找到最短的转换路径。

```cpp
int ladderLength(string beginWord, string endWord, vector<string>& wordList) {
    unordered_set<string> wordSet(wordList.begin(), wordList.end());
    if (wordSet.find(endWord) == wordSet.end()) return 0;
    
    queue<string> q;
    q.push(beginWord);
    int level = 1;
    
    while (!q.empty()) {
        int size = q.size();
        
        for (int i = 0; i < size; i++) {
            string current = q.front();
            q.pop();
            
            if (current == endWord) return level;
            
            // 尝试改变每个字符
            for (int j = 0; j < current.length(); j++) {
                char original = current[j];
                
                for (char c = 'a'; c <= 'z'; c++) {
                    if (c == original) continue;
                    
                    current[j] = c;
                    
                    if (wordSet.find(current) != wordSet.end()) {
                        q.push(current);
                        wordSet.erase(current);  // 避免重复访问
                    }
                }
                
                current[j] = original;  // 恢复原字符
            }
        }
        
        level++;
    }
    
    return 0;  // 无法转换
}
```

### 例题4: 网络延迟时间 - 传播游戏 📡
**题目**: 给定网络中的节点和边，计算信号从节点K传播到所有其他节点所需的时间。

**生活例子**: 就像网络传播，信号从中心节点开始，通过边传播到其他节点。

```cpp
int networkDelayTime(vector<vector<int>>& times, int n, int k) {
    vector<vector<pair<int, int>>> graph(n + 1);
    
    // 构建图
    for (auto& time : times) {
        graph[time[0]].push_back({time[1], time[2]});
    }
    
    vector<int> distance(n + 1, INT_MAX);
    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> pq;
    
    distance[k] = 0;
    pq.push({0, k});
    
    while (!pq.empty()) {
        int current = pq.top().second;
        pq.pop();
        
        for (auto& edge : graph[current]) {
            int neighbor = edge.first;
            int weight = edge.second;
            
            if (distance[current] + weight < distance[neighbor]) {
                distance[neighbor] = distance[current] + weight;
                pq.push({distance[neighbor], neighbor});
            }
        }
    }
    
    int maxTime = 0;
    for (int i = 1; i <= n; i++) {
        if (distance[i] == INT_MAX) return -1;  // 无法到达
        maxTime = max(maxTime, distance[i]);
    }
    
    return maxTime;
}
```

### 例题5: 最小高度树 - 找中心游戏 🎯
**题目**: 给定一个树，找到所有可能的根节点，使得以该节点为根的树高度最小。

**生活例子**: 就像找班级的中心人物，以他为中心，到所有人的距离都最短。

```cpp
vector<int> findMinHeightTrees(int n, vector<vector<int>>& edges) {
    if (n == 1) return {0};
    
    vector<vector<int>> graph(n);
    vector<int> degree(n, 0);
    
    // 构建图和度数
    for (auto& edge : edges) {
        graph[edge[0]].push_back(edge[1]);
        graph[edge[1]].push_back(edge[0]);
        degree[edge[0]]++;
        degree[edge[1]]++;
    }
    
    queue<int> q;
    
    // 将叶子节点入队
    for (int i = 0; i < n; i++) {
        if (degree[i] == 1) {
            q.push(i);
        }
    }
    
    while (n > 2) {
        int size = q.size();
        n -= size;
        
        for (int i = 0; i < size; i++) {
            int leaf = q.front();
            q.pop();
            
            for (int neighbor : graph[leaf]) {
                degree[neighbor]--;
                if (degree[neighbor] == 1) {
                    q.push(neighbor);
                }
            }
        }
    }
    
    vector<int> result;
    while (!q.empty()) {
        result.push_back(q.front());
        q.pop();
    }
    
    return result;
}
```

**例题总结**:
- 课程表：选课游戏，拓扑排序解决 📚
- 岛屿数量：数岛屿游戏，DFS遍历 🏝️
- 单词接龙：接龙游戏，BFS找最短路径 🔗
- 网络延迟：传播游戏，Dijkstra算法 📡
- 最小高度树：找中心游戏，拓扑排序 🎯

---

## 9. 挑战练习 🏆

### 基础练习 - 新手村任务 🎯
1. **图的基本操作 - 网络新手**
   - 图的表示和遍历
   - 连通性检测
   - 环检测
   - 路径查找

2. **最短路径 - 导航新手**
   - Dijkstra算法
   - Floyd-Warshall算法
   - 单源最短路径
   - 所有点对最短路径

3. **最小生成树 - 连接新手**
   - Kruskal算法
   - Prim算法
   - 最小生成树应用
   - 网络设计问题

### 进阶练习 - 冒险者任务 ⚔️
1. **拓扑排序 - 排序专家**
   - 课程安排问题
   - 任务调度问题
   - 依赖关系管理
   - 编译顺序问题

2. **图的高级应用 - 应用专家**
   - 强连通分量
   - 二分图匹配
   - 网络流问题
   - 图着色问题

3. **复杂图算法 - 算法专家**
   - 欧拉路径
   - 哈密顿路径
   - 旅行商问题
   - 最小割问题

### 高级练习 - 王者挑战 👑
1. **图论综合应用 - 综合大师**
   - 社交网络分析
   - 交通网络优化
   - 游戏地图设计
   - 推荐系统设计

2. **图论优化算法 - 优化专家**
   - 近似算法
   - 启发式算法
   - 并行图算法
   - 动态图算法

3. **图论前沿问题 - 前沿探索者**
   - 图神经网络
   - 图数据库
   - 图挖掘算法
   - 图可视化

---

## 学习建议 - 成为图论高手 🎓

### 1. 理解图论本质 - 掌握核心思想 🧠
- **图的结构**: 理解顶点、边、权重等基本概念
- **图的性质**: 理解连通性、环、路径等性质
- **图的分类**: 理解有向图、无向图、有权图等分类
- **图的应用**: 理解图论在实际问题中的应用

### 2. 掌握图算法 - 熟练编程 💻
- **遍历算法**: 掌握DFS和BFS的实现
- **最短路径**: 掌握Dijkstra和Floyd-Warshall算法
- **最小生成树**: 掌握Kruskal和Prim算法
- **拓扑排序**: 掌握Kahn和DFS拓扑排序

### 3. 学会图的应用 - 解决实际问题 🛠️
- **网络分析**: 分析社交网络、交通网络
- **路径规划**: 设计导航系统、游戏AI
- **优化问题**: 解决物流、调度等问题
- **系统设计**: 设计分布式系统、数据库

### 4. 大量练习 - 熟能生巧 🏃‍♂️
- **从简单开始**: 先做基础的图遍历问题
- **逐步提高**: 再做复杂的图算法问题
- **总结模板**: 记住常用的图算法模板
- **分析复杂度**: 理解算法的时间和空间复杂度

---

## 课后作业 - 巩固练习 📋

### 必做作业（基础）
1. **图的基本操作**
   - 实现图的邻接矩阵和邻接表表示
   - 实现DFS和BFS遍历
   - 检测图的连通性
   - 查找两个顶点之间的路径

2. **最短路径算法**
   - 实现Dijkstra算法
   - 实现Floyd-Warshall算法
   - 解决单源最短路径问题
   - 解决所有点对最短路径问题

3. **最小生成树算法**
   - 实现Kruskal算法
   - 实现Prim算法
   - 解决最小生成树问题
   - 应用最小生成树解决实际问题

### 选做作业（进阶）
4. **拓扑排序**
   - 实现Kahn算法
   - 实现DFS拓扑排序
   - 解决课程安排问题
   - 解决任务调度问题

5. **图的高级应用**
   - 实现强连通分量算法
   - 解决二分图匹配问题
   - 实现网络流算法
   - 解决图着色问题

### 挑战作业（高手）
6. **综合应用**
   - 设计社交网络分析系统
   - 实现交通网络优化算法
   - 开发游戏地图生成器
   - 构建推荐系统

---

## 学习进度检查 ✅

### 第一周目标
- [ ] 掌握图的基本概念和表示方法
- [ ] 实现图的遍历算法
- [ ] 理解最短路径算法
- [ ] 完成基础练习题

### 第二周目标
- [ ] 掌握最小生成树算法
- [ ] 理解拓扑排序算法
- [ ] 实现图的高级应用
- [ ] 完成进阶练习题

### 第三周目标
- [ ] 掌握复杂图算法
- [ ] 解决综合应用问题
- [ ] 优化算法效率
- [ ] 挑战高级题目

---

## 学习资源推荐 📖

### 在线练习平台
- **LeetCode**: 图论专题练习
- **洛谷**: 适合初学者的图论题目
- **Codeforces**: 竞赛级别的图论问题
- **AtCoder**: 高质量的图论算法题

### 学习网站
- **算法导论**: 图算法基础理论
- **数据结构与算法分析**: 详细的图算法实现
- **算法竞赛入门经典**: 图论实战
- **算法竞赛进阶指南**: 高级图论技巧

### 推荐书籍
- 《算法导论》- 图算法章节
- 《数据结构与算法分析》- 图论章节
- 《算法竞赛入门经典》- 图论算法
- 《算法竞赛进阶指南》- 图论优化

---

## 图论记忆口诀 🎵

### 图的基本概念口诀
"顶点边组成图，有向无向有权重，度路径环连通，图论无处不在！"

### 图的表示方法口诀
"邻接矩阵表格法，邻接表链表法，稠密用矩阵，稀疏用链表！"

### 图的遍历口诀
"DFS一条路走到黑，BFS齐头并进，遍历探索网络！"

### 最短路径口诀
"Dijkstra贪心单源，Floyd动态全对，最短路径导航用！"

### 最小生成树口诀
"Kruskal贪心选边，Prim贪心选点，连接所有点权重最小！"

### 拓扑排序口诀
"Kahn入度统计法，DFS深度优先法，有向无环图排序！"

### 图论应用口诀
"社交交通网络游戏，图论无处不在，连接世界优化路径！"

---

**记住**: 图论就像连接世界的网络，帮助我们理解复杂的关系，找到最优的路径！掌握了图论，你就能在编程的世界里自由探索！加油，未来的图论大师！🚀✨
