# 专题5: 动态规划讲义 🎯
## 初一学生专用版 - 聪明的记忆大师！

## 课程信息
- **授课老师**: 孔维玲 (东师附中国际金牌教练)
- **课程时间**: 10月6日 13:30-16:30
- **目标学生**: 初一学生，准备冲刺CSP-J高分
- **课程性质**: B层训练营核心课程

---

## 目录
1. [动态规划是什么？](#1-动态规划是什么)
2. [动态规划的核心思想 - 记忆化搜索](#2-动态规划的核心思想---记忆化搜索)
3. [动态规划的基本步骤 - 四步法](#3-动态规划的基本步骤---四步法)
4. [一维动态规划 - 线性问题](#4-一维动态规划---线性问题)
5. [二维动态规划 - 表格问题](#5-二维动态规划---表格问题)
6. [背包问题 - 装包游戏](#6-背包问题---装包游戏)
7. [区间动态规划 - 分段问题](#7-区间动态规划---分段问题)
8. [状态压缩动态规划 - 状态游戏](#8-状态压缩动态规划---状态游戏)
9. [趣味例题](#9-趣味例题)
10. [挑战练习](#10-挑战练习)

---

## 1. 动态规划是什么？

### 1.1 动态规划就像聪明的记忆大师 🧠
**核心原理**: 动态规划就是记住已经算过的结果，避免重复计算，就像聪明的学生记住做过的题目答案！

**简单理解**: 动态规划 = 记忆化 + 最优子结构

**生活例子**: 就像做数学题，如果已经算过1+2+3=6，下次遇到就不用再算一遍，直接记住答案。

### 1.2 动态规划的特点 ✨
- **重叠子问题**: 大问题包含相同的小问题
- **最优子结构**: 大问题的最优解包含小问题的最优解
- **无后效性**: 当前状态只依赖于之前的状态

**生活例子**: 就像爬楼梯，要爬到第10层，可以先爬到第8层，再爬2层。爬到第8层的方法数已经算过了，就不用再算。

### 1.3 动态规划 vs 其他算法 ⚖️
- **vs 递归**: 动态规划避免重复计算，递归会重复计算
- **vs 贪心**: 动态规划考虑所有可能，贪心只考虑当前最优
- **vs 分治**: 动态规划有重叠子问题，分治没有重叠子问题

**记忆口诀**: "重叠子问题，最优子结构，无后效性，记忆化搜索！"

---

## 2. 动态规划的核心思想 - 记忆化搜索 💾

### 2.1 从递归到动态规划 🔄
**核心原理**: 动态规划就是从递归优化而来的，通过记忆化避免重复计算！

**简单理解**: 递归 + 记忆化 = 动态规划

**生活例子**: 就像背单词，第一次查字典记住意思，下次遇到就不用再查了。

### 2.2 斐波那契数列的进化 🐰
```cpp
// 普通递归（很慢）
int fibonacci(int n) {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);  // 会重复计算
}

// 记忆化递归（快一些）
int memo[1000];
int fibonacciMemo(int n) {
    if (n <= 1) return n;
    if (memo[n] != -1) return memo[n];  // 已经算过了
    return memo[n] = fibonacciMemo(n - 1) + fibonacciMemo(n - 2);
}

// 动态规划（最快）
int fibonacciDP(int n) {
    if (n <= 1) return n;
    int a = 0, b = 1;
    for (int i = 2; i <= n; i++) {
        int temp = a + b;
        a = b;
        b = temp;
    }
    return b;
}
```

### 2.3 记忆化的好处 🎯
- **避免重复计算**: 每个子问题只算一次
- **提高效率**: 时间复杂度从指数降到线性
- **节省时间**: 大大减少计算时间

**记忆化记忆口诀**: "递归重复计算慢，记忆化避免重复，动态规划效率高！"

---

## 3. 动态规划的基本步骤 - 四步法 📋

### 3.1 动态规划四步法 🎯
1. **定义状态**: 确定dp数组的含义
2. **状态转移**: 找出状态之间的关系
3. **初始状态**: 确定边界条件
4. **计算顺序**: 确定计算顺序

**生活例子**: 就像做菜，先准备食材（定义状态），再按步骤做（状态转移），注意火候（初始状态），按顺序完成（计算顺序）。

### 3.2 四步法详解 📝

#### 3.2.1 定义状态 - 准备食材
```cpp
// 爬楼梯问题：dp[i] 表示爬到第i层的方法数
int dp[n + 1];

// 最长递增子序列：dp[i] 表示以第i个元素结尾的最长递增子序列长度
int dp[n];

// 背包问题：dp[i][j] 表示前i个物品，容量为j时的最大价值
int dp[n + 1][capacity + 1];
```

#### 3.2.2 状态转移 - 按步骤做
```cpp
// 爬楼梯：dp[i] = dp[i-1] + dp[i-2]
dp[i] = dp[i - 1] + dp[i - 2];

// 最长递增子序列：dp[i] = max(dp[j] + 1) for all j < i and nums[j] < nums[i]
for (int j = 0; j < i; j++) {
    if (nums[j] < nums[i]) {
        dp[i] = max(dp[i], dp[j] + 1);
    }
}

// 背包问题：dp[i][j] = max(dp[i-1][j], dp[i-1][j-weight[i]] + value[i])
dp[i][j] = max(dp[i - 1][j], dp[i - 1][j - weight[i]] + value[i]);
```

#### 3.2.3 初始状态 - 注意火候
```cpp
// 爬楼梯：dp[0] = 1, dp[1] = 1
dp[0] = 1;
dp[1] = 1;

// 最长递增子序列：dp[i] = 1 for all i
for (int i = 0; i < n; i++) {
    dp[i] = 1;
}

// 背包问题：dp[0][j] = 0 for all j
for (int j = 0; j <= capacity; j++) {
    dp[0][j] = 0;
}
```

#### 3.2.4 计算顺序 - 按顺序完成
```cpp
// 爬楼梯：从前往后
for (int i = 2; i <= n; i++) {
    dp[i] = dp[i - 1] + dp[i - 2];
}

// 最长递增子序列：从前往后
for (int i = 1; i < n; i++) {
    for (int j = 0; j < i; j++) {
        if (nums[j] < nums[i]) {
            dp[i] = max(dp[i], dp[j] + 1);
        }
    }
}

// 背包问题：从前往后
for (int i = 1; i <= n; i++) {
    for (int j = 1; j <= capacity; j++) {
        dp[i][j] = max(dp[i - 1][j], dp[i - 1][j - weight[i]] + value[i]);
    }
}
```

**四步法记忆口诀**: "定义状态准备食材，状态转移按步骤做，初始状态注意火候，计算顺序按顺序完成！"

---

## 4. 一维动态规划 - 线性问题 📏

### 4.1 爬楼梯问题 - 上楼游戏 🏢
**问题描述**: 每次可以爬1或2个台阶，问有多少种方法爬到第n层。

**生活例子**: 就像爬楼梯，每次可以跨1步或2步，问有多少种方法爬到楼顶。

```cpp
int climbStairs(int n) {
    if (n <= 2) return n;
    
    int dp[n + 1];
    dp[1] = 1;  // 爬到第1层有1种方法
    dp[2] = 2;  // 爬到第2层有2种方法
    
    for (int i = 3; i <= n; i++) {
        dp[i] = dp[i - 1] + dp[i - 2];  // 从第i-1层或第i-2层爬上来
    }
    
    return dp[n];
}

// 空间优化版本
int climbStairsOptimized(int n) {
    if (n <= 2) return n;
    
    int a = 1, b = 2;  // 只需要保存前两个状态
    for (int i = 3; i <= n; i++) {
        int temp = a + b;
        a = b;
        b = temp;
    }
    
    return b;
}
```

### 4.2 最长递增子序列 - 排队游戏 📏
**问题描述**: 给定数组，找到最长严格递增子序列的长度。

**生活例子**: 就像排队，要找到最长的"从矮到高"的队伍。

```cpp
int lengthOfLIS(vector<int>& nums) {
    int n = nums.size();
    if (n == 0) return 0;
    
    vector<int> dp(n, 1);  // dp[i] 表示以nums[i]结尾的最长递增子序列长度
    
    for (int i = 1; i < n; i++) {
        for (int j = 0; j < i; j++) {
            if (nums[j] < nums[i]) {  // 如果nums[j] < nums[i]
                dp[i] = max(dp[i], dp[j] + 1);  // 可以接在nums[j]后面
            }
        }
    }
    
    return *max_element(dp.begin(), dp.end());  // 返回最大值
}

// 二分查找优化版本
int lengthOfLISOptimized(vector<int>& nums) {
    vector<int> tails;  // tails[i] 表示长度为i+1的递增子序列的最小末尾元素
    
    for (int num : nums) {
        auto it = lower_bound(tails.begin(), tails.end(), num);
        if (it == tails.end()) {
            tails.push_back(num);  // 开始新的递增子序列
        } else {
            *it = num;  // 更新更优的末尾元素
        }
    }
    
    return tails.size();
}
```

### 4.3 打家劫舍 - 偷盗游戏 🏠
**问题描述**: 不能偷相邻的房子，问最多能偷多少钱。

**生活例子**: 就像偷盗游戏，不能偷相邻的房子，问最多能偷多少钱。

```cpp
int rob(vector<int>& nums) {
    int n = nums.size();
    if (n == 0) return 0;
    if (n == 1) return nums[0];
    
    vector<int> dp(n);
    dp[0] = nums[0];  // 只偷第0个房子
    dp[1] = max(nums[0], nums[1]);  // 偷第0个或第1个房子
    
    for (int i = 2; i < n; i++) {
        dp[i] = max(dp[i - 1], dp[i - 2] + nums[i]);  // 不偷第i个房子 或 偷第i个房子
    }
    
    return dp[n - 1];
}

// 空间优化版本
int robOptimized(vector<int>& nums) {
    int n = nums.size();
    if (n == 0) return 0;
    if (n == 1) return nums[0];
    
    int a = nums[0], b = max(nums[0], nums[1]);  // 只需要保存前两个状态
    for (int i = 2; i < n; i++) {
        int temp = max(b, a + nums[i]);
        a = b;
        b = temp;
    }
    
    return b;
}
```

### 4.4 一维动态规划的特点 ✨
- **状态定义**: 通常是一维数组
- **状态转移**: 通常依赖前几个状态
- **空间优化**: 可以用滚动数组优化空间

**一维动态规划记忆口诀**: "一维数组存状态，依赖前几个状态，空间优化用滚动！"

---

## 5. 二维动态规划 - 表格问题 📊

### 5.1 最长公共子序列 - 找共同朋友 👫
**问题描述**: 给定两个字符串，找到它们的最长公共子序列。

**生活例子**: 就像找两个班级的共同朋友，看最多有多少个共同朋友。

```cpp
int longestCommonSubsequence(string text1, string text2) {
    int m = text1.length(), n = text2.length();
    vector<vector<int>> dp(m + 1, vector<int>(n + 1, 0));
    
    for (int i = 1; i <= m; i++) {
        for (int j = 1; j <= n; j++) {
            if (text1[i - 1] == text2[j - 1]) {  // 字符相同
                dp[i][j] = dp[i - 1][j - 1] + 1;  // 长度+1
            } else {  // 字符不同
                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1]);  // 取最大值
            }
        }
    }
    
    return dp[m][n];
}

// 空间优化版本
int longestCommonSubsequenceOptimized(string text1, string text2) {
    int m = text1.length(), n = text2.length();
    vector<int> prev(n + 1, 0), curr(n + 1, 0);
    
    for (int i = 1; i <= m; i++) {
        for (int j = 1; j <= n; j++) {
            if (text1[i - 1] == text2[j - 1]) {
                curr[j] = prev[j - 1] + 1;
            } else {
                curr[j] = max(prev[j], curr[j - 1]);
            }
        }
        prev = curr;
    }
    
    return curr[n];
}
```

### 5.2 编辑距离 - 改错别字 ✏️
**问题描述**: 给定两个字符串，计算将一个字符串转换为另一个字符串的最少操作次数。

**生活例子**: 就像改错别字，计算把一个词改成另一个词需要多少次操作。

```cpp
int minDistance(string word1, string word2) {
    int m = word1.length(), n = word2.length();
    vector<vector<int>> dp(m + 1, vector<int>(n + 1, 0));
    
    // 初始化边界条件
    for (int i = 0; i <= m; i++) dp[i][0] = i;  // 删除i个字符
    for (int j = 0; j <= n; j++) dp[0][j] = j;  // 插入j个字符
    
    for (int i = 1; i <= m; i++) {
        for (int j = 1; j <= n; j++) {
            if (word1[i - 1] == word2[j - 1]) {  // 字符相同
                dp[i][j] = dp[i - 1][j - 1];  // 不需要操作
            } else {  // 字符不同
                dp[i][j] = min({
                    dp[i - 1][j] + 1,      // 删除
                    dp[i][j - 1] + 1,      // 插入
                    dp[i - 1][j - 1] + 1   // 替换
                });
            }
        }
    }
    
    return dp[m][n];
}
```

### 5.3 不同路径 - 走迷宫游戏 🗺️
**问题描述**: 从左上角到右下角，只能向右或向下走，问有多少种不同的路径。

**生活例子**: 就像走迷宫，只能向右或向下走，问有多少种不同的路径。

```cpp
int uniquePaths(int m, int n) {
    vector<vector<int>> dp(m, vector<int>(n, 1));  // 初始化为1
    
    for (int i = 1; i < m; i++) {
        for (int j = 1; j < n; j++) {
            dp[i][j] = dp[i - 1][j] + dp[i][j - 1];  // 从上方或左方来
        }
    }
    
    return dp[m - 1][n - 1];
}

// 空间优化版本
int uniquePathsOptimized(int m, int n) {
    vector<int> prev(n, 1), curr(n, 1);
    
    for (int i = 1; i < m; i++) {
        for (int j = 1; j < n; j++) {
            curr[j] = prev[j] + curr[j - 1];
        }
        prev = curr;
    }
    
    return curr[n - 1];
}
```

### 5.4 二维动态规划的特点 ✨
- **状态定义**: 通常是二维数组
- **状态转移**: 通常依赖上方、左方或左上方的状态
- **空间优化**: 可以用滚动数组优化空间

**二维动态规划记忆口诀**: "二维数组存状态，依赖上左左上状态，空间优化用滚动！"

---

## 6. 背包问题 - 装包游戏 🎒

### 6.1 0-1背包问题 - 装包游戏 🎒
**问题描述**: 有n个物品，每个物品有重量和价值，背包容量有限，问最多能装多少价值。

**生活例子**: 就像装包游戏，每个物品有重量和价值，背包容量有限，问最多能装多少价值。

```cpp
int knapsack01(vector<int>& weights, vector<int>& values, int capacity) {
    int n = weights.size();
    vector<vector<int>> dp(n + 1, vector<int>(capacity + 1, 0));
    
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= capacity; j++) {
            if (weights[i - 1] <= j) {  // 可以装下第i个物品
                dp[i][j] = max(
                    dp[i - 1][j],  // 不装第i个物品
                    dp[i - 1][j - weights[i - 1]] + values[i - 1]  // 装第i个物品
                );
            } else {  // 装不下第i个物品
                dp[i][j] = dp[i - 1][j];
            }
        }
    }
    
    return dp[n][capacity];
}

// 空间优化版本
int knapsack01Optimized(vector<int>& weights, vector<int>& values, int capacity) {
    vector<int> dp(capacity + 1, 0);
    
    for (int i = 0; i < weights.size(); i++) {
        for (int j = capacity; j >= weights[i]; j--) {  // 从后往前遍历
            dp[j] = max(dp[j], dp[j - weights[i]] + values[i]);
        }
    }
    
    return dp[capacity];
}
```

### 6.2 完全背包问题 - 无限装包游戏 ♾️
**问题描述**: 每个物品可以无限使用，问最多能装多少价值。

**生活例子**: 就像无限装包游戏，每个物品可以无限使用，问最多能装多少价值。

```cpp
int knapsackComplete(vector<int>& weights, vector<int>& values, int capacity) {
    vector<int> dp(capacity + 1, 0);
    
    for (int i = 0; i < weights.size(); i++) {
        for (int j = weights[i]; j <= capacity; j++) {  // 从前往后遍历
            dp[j] = max(dp[j], dp[j - weights[i]] + values[i]);
        }
    }
    
    return dp[capacity];
}
```

### 6.3 多重背包问题 - 有限装包游戏 🔢
**问题描述**: 每个物品有数量限制，问最多能装多少价值。

**生活例子**: 就像有限装包游戏，每个物品有数量限制，问最多能装多少价值。

```cpp
int knapsackMultiple(vector<int>& weights, vector<int>& values, vector<int>& counts, int capacity) {
    vector<int> dp(capacity + 1, 0);
    
    for (int i = 0; i < weights.size(); i++) {
        for (int j = capacity; j >= weights[i]; j--) {
            for (int k = 1; k <= counts[i] && k * weights[i] <= j; k++) {
                dp[j] = max(dp[j], dp[j - k * weights[i]] + k * values[i]);
            }
        }
    }
    
    return dp[capacity];
}
```

### 6.4 背包问题的特点 ✨
- **状态定义**: dp[i][j] 表示前i个物品，容量为j时的最大价值
- **状态转移**: 考虑装或不装当前物品
- **空间优化**: 可以用一维数组优化空间

**背包问题记忆口诀**: "0-1背包从后往前，完全背包从前往后，多重背包三重循环！"

---

## 7. 区间动态规划 - 分段问题 📏

### 7.1 矩阵链乘法 - 计算顺序游戏 🧮
**问题描述**: 给定矩阵链，找到最优的乘法顺序，使计算次数最少。

**生活例子**: 就像计算矩阵乘法，不同的计算顺序会影响计算次数，要找最优顺序。

```cpp
int matrixChainMultiplication(vector<int>& dimensions) {
    int n = dimensions.size() - 1;  // 矩阵个数
    vector<vector<int>> dp(n, vector<int>(n, 0));
    
    // 计算不同长度的矩阵链
    for (int length = 2; length <= n; length++) {
        for (int i = 0; i <= n - length; i++) {
            int j = i + length - 1;
            dp[i][j] = INT_MAX;
            
            // 尝试不同的分割点
            for (int k = i; k < j; k++) {
                int cost = dp[i][k] + dp[k + 1][j] + 
                          dimensions[i] * dimensions[k + 1] * dimensions[j + 1];
                dp[i][j] = min(dp[i][j], cost);
            }
        }
    }
    
    return dp[0][n - 1];
}
```

### 7.2 回文子串 - 对称游戏 🪞
**问题描述**: 给定字符串，计算有多少个回文子串。

**生活例子**: 就像找对称的字符串，看有多少个回文子串。

```cpp
int countSubstrings(string s) {
    int n = s.length();
    vector<vector<bool>> dp(n, vector<bool>(n, false));
    int count = 0;
    
    // 单个字符都是回文
    for (int i = 0; i < n; i++) {
        dp[i][i] = true;
        count++;
    }
    
    // 两个字符的情况
    for (int i = 0; i < n - 1; i++) {
        if (s[i] == s[i + 1]) {
            dp[i][i + 1] = true;
            count++;
        }
    }
    
    // 三个及以上字符的情况
    for (int length = 3; length <= n; length++) {
        for (int i = 0; i <= n - length; i++) {
            int j = i + length - 1;
            if (s[i] == s[j] && dp[i + 1][j - 1]) {
                dp[i][j] = true;
                count++;
            }
        }
    }
    
    return count;
}
```

### 7.3 区间动态规划的特点 ✨
- **状态定义**: dp[i][j] 表示区间[i,j]的最优解
- **状态转移**: 通常考虑区间的分割点
- **计算顺序**: 按区间长度从小到大计算

**区间动态规划记忆口诀**: "区间DP按长度，分割点找最优解，回文矩阵链乘法！"

---

## 8. 状态压缩动态规划 - 状态游戏 🎮

### 8.1 状态压缩的概念 🔧
**核心原理**: 用二进制数表示状态，每个位表示一个元素的状态！

**简单理解**: 状态压缩 = 二进制 + 位运算

**生活例子**: 就像开关控制，每个开关有开和关两种状态，用0和1表示。

### 8.2 旅行商问题 - 周游世界游戏 🌍
**问题描述**: 访问所有城市一次，回到起点，问最短路径。

**生活例子**: 就像周游世界，要访问所有城市一次，回到起点，问最短路径。

```cpp
int tsp(vector<vector<int>>& distances) {
    int n = distances.size();
    vector<vector<int>> dp(1 << n, vector<int>(n, INT_MAX));
    
    dp[1][0] = 0;  // 从城市0开始
    
    for (int mask = 1; mask < (1 << n); mask++) {
        for (int u = 0; u < n; u++) {
            if (!(mask & (1 << u))) continue;  // 城市u不在当前状态中
            
            for (int v = 0; v < n; v++) {
                if (mask & (1 << v)) continue;  // 城市v已经在当前状态中
                
                int newMask = mask | (1 << v);
                dp[newMask][v] = min(dp[newMask][v], dp[mask][u] + distances[u][v]);
            }
        }
    }
    
    int result = INT_MAX;
    for (int i = 1; i < n; i++) {
        result = min(result, dp[(1 << n) - 1][i] + distances[i][0]);
    }
    
    return result;
}
```

### 8.3 状态压缩动态规划的特点 ✨
- **状态表示**: 用二进制数表示状态
- **位运算**: 用位运算操作状态
- **适用场景**: 状态数量较少的问题

**状态压缩记忆口诀**: "二进制表示状态，位运算操作状态，旅行商问题经典！"

---

## 9. 趣味例题 🎮

### 例题1: 爬楼梯 - 上楼游戏 🏢
**题目**: 每次可以爬1或2个台阶，问有多少种方法爬到第n层。

**生活例子**: 就像爬楼梯，每次可以跨1步或2步，问有多少种方法爬到楼顶。

```cpp
int climbStairs(int n) {
    if (n <= 2) return n;
    
    int a = 1, b = 2;
    for (int i = 3; i <= n; i++) {
        int temp = a + b;
        a = b;
        b = temp;
    }
    
    return b;
}
```

### 例题2: 最长递增子序列 - 排队游戏 📏
**题目**: 给定数组，找到最长严格递增子序列的长度。

**生活例子**: 就像排队，要找到最长的"从矮到高"的队伍。

```cpp
int lengthOfLIS(vector<int>& nums) {
    vector<int> tails;
    
    for (int num : nums) {
        auto it = lower_bound(tails.begin(), tails.end(), num);
        if (it == tails.end()) {
            tails.push_back(num);
        } else {
            *it = num;
        }
    }
    
    return tails.size();
}
```

### 例题3: 打家劫舍 - 偷盗游戏 🏠
**题目**: 不能偷相邻的房子，问最多能偷多少钱。

**生活例子**: 就像偷盗游戏，不能偷相邻的房子，问最多能偷多少钱。

```cpp
int rob(vector<int>& nums) {
    int n = nums.size();
    if (n == 0) return 0;
    if (n == 1) return nums[0];
    
    int a = nums[0], b = max(nums[0], nums[1]);
    for (int i = 2; i < n; i++) {
        int temp = max(b, a + nums[i]);
        a = b;
        b = temp;
    }
    
    return b;
}
```

### 例题4: 最长公共子序列 - 找共同朋友 👫
**题目**: 给定两个字符串，找到它们的最长公共子序列。

**生活例子**: 就像找两个班级的共同朋友，看最多有多少个共同朋友。

```cpp
int longestCommonSubsequence(string text1, string text2) {
    int m = text1.length(), n = text2.length();
    vector<vector<int>> dp(m + 1, vector<int>(n + 1, 0));
    
    for (int i = 1; i <= m; i++) {
        for (int j = 1; j <= n; j++) {
            if (text1[i - 1] == text2[j - 1]) {
                dp[i][j] = dp[i - 1][j - 1] + 1;
            } else {
                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1]);
            }
        }
    }
    
    return dp[m][n];
}
```

### 例题5: 0-1背包问题 - 装包游戏 🎒
**题目**: 有n个物品，每个物品有重量和价值，背包容量有限，问最多能装多少价值。

**生活例子**: 就像装包游戏，每个物品有重量和价值，背包容量有限，问最多能装多少价值。

```cpp
int knapsack01(vector<int>& weights, vector<int>& values, int capacity) {
    vector<int> dp(capacity + 1, 0);
    
    for (int i = 0; i < weights.size(); i++) {
        for (int j = capacity; j >= weights[i]; j--) {
            dp[j] = max(dp[j], dp[j - weights[i]] + values[i]);
        }
    }
    
    return dp[capacity];
}
```

**例题总结**:
- 爬楼梯：上楼游戏，斐波那契数列 🏢
- 最长递增子序列：排队游戏，二分查找优化 📏
- 打家劫舍：偷盗游戏，空间优化 🏠
- 最长公共子序列：找共同朋友，二维DP 👫
- 0-1背包：装包游戏，空间优化 🎒

---

## 10. 挑战练习 🏆

### 基础练习 - 新手村任务 🎯
1. **一维动态规划 - 线性新手**
   - 爬楼梯问题
   - 最长递增子序列
   - 打家劫舍问题
   - 斐波那契数列

2. **二维动态规划 - 表格新手**
   - 最长公共子序列
   - 编辑距离
   - 不同路径
   - 最小路径和

3. **背包问题 - 装包新手**
   - 0-1背包问题
   - 完全背包问题
   - 多重背包问题
   - 分组背包问题

### 进阶练习 - 冒险者任务 ⚔️
1. **区间动态规划 - 分段专家**
   - 矩阵链乘法
   - 回文子串
   - 回文子序列
   - 石子合并

2. **状态压缩动态规划 - 状态专家**
   - 旅行商问题
   - 数独求解
   - 棋盘覆盖
   - 状态机问题

3. **树形动态规划 - 树形专家**
   - 树的直径
   - 树的最大独立集
   - 树的最小支配集
   - 树上背包

### 高级练习 - 王者挑战 👑
1. **动态规划优化 - 优化专家**
   - 斜率优化
   - 四边形不等式优化
   - 单调队列优化
   - 滚动数组优化

2. **动态规划综合应用 - 综合大师**
   - 股票买卖问题
   - 字符串匹配问题
   - 数位动态规划
   - 概率动态规划

3. **动态规划前沿问题 - 前沿探索者**
   - 强化学习
   - 动态规划与机器学习
   - 近似动态规划
   - 随机动态规划

---

## 学习建议 - 成为动态规划高手 🎓

### 1. 理解动态规划本质 - 掌握核心思想 🧠
- **重叠子问题**: 理解为什么需要记忆化
- **最优子结构**: 理解大问题与小问题的关系
- **无后效性**: 理解状态转移的条件
- **状态设计**: 理解如何设计状态

### 2. 掌握动态规划技巧 - 熟练编程 💻
- **四步法**: 掌握动态规划的基本步骤
- **状态转移**: 掌握如何写出状态转移方程
- **边界条件**: 掌握如何设置初始状态
- **空间优化**: 掌握如何优化空间复杂度

### 3. 学会动态规划应用 - 解决实际问题 🛠️
- **一维问题**: 解决线性动态规划问题
- **二维问题**: 解决表格动态规划问题
- **背包问题**: 解决各种背包问题
- **区间问题**: 解决区间动态规划问题

### 4. 大量练习 - 熟能生巧 🏃‍♂️
- **从简单开始**: 先做基础的动态规划问题
- **逐步提高**: 再做复杂的动态规划问题
- **总结模板**: 记住常用的动态规划模板
- **分析复杂度**: 理解算法的时间和空间复杂度

---

## 课后作业 - 巩固练习 📋

### 必做作业（基础）
1. **一维动态规划练习**
   - 实现爬楼梯问题
   - 解决最长递增子序列问题
   - 实现打家劫舍问题
   - 解决斐波那契数列问题

2. **二维动态规划练习**
   - 实现最长公共子序列
   - 解决编辑距离问题
   - 实现不同路径问题
   - 解决最小路径和问题

3. **背包问题练习**
   - 实现0-1背包问题
   - 解决完全背包问题
   - 实现多重背包问题
   - 解决分组背包问题

### 选做作业（进阶）
4. **区间动态规划练习**
   - 实现矩阵链乘法
   - 解决回文子串问题
   - 实现回文子序列
   - 解决石子合并问题

5. **状态压缩动态规划练习**
   - 实现旅行商问题
   - 解决数独求解问题
   - 实现棋盘覆盖问题
   - 解决状态机问题

### 挑战作业（高手）
6. **综合应用**
   - 解决股票买卖问题
   - 实现字符串匹配问题
   - 解决数位动态规划问题
   - 挑战概率动态规划问题

---

## 学习进度检查 ✅

### 第一周目标
- [ ] 掌握动态规划的基本概念和四步法
- [ ] 实现一维动态规划问题
- [ ] 理解二维动态规划问题
- [ ] 完成基础练习题

### 第二周目标
- [ ] 掌握背包问题的各种变种
- [ ] 理解区间动态规划问题
- [ ] 实现状态压缩动态规划
- [ ] 完成进阶练习题

### 第三周目标
- [ ] 掌握动态规划优化技巧
- [ ] 解决综合应用问题
- [ ] 优化算法效率
- [ ] 挑战高级题目

---

## 学习资源推荐 📖

### 在线练习平台
- **LeetCode**: 动态规划专题练习
- **洛谷**: 适合初学者的动态规划题目
- **Codeforces**: 竞赛级别的动态规划问题
- **AtCoder**: 高质量的动态规划算法题

### 学习网站
- **算法导论**: 动态规划基础理论
- **数据结构与算法分析**: 详细的动态规划实现
- **算法竞赛入门经典**: 动态规划实战
- **算法竞赛进阶指南**: 高级动态规划技巧

### 推荐书籍
- 《算法导论》- 动态规划章节
- 《数据结构与算法分析》- 动态规划章节
- 《算法竞赛入门经典》- 动态规划算法
- 《算法竞赛进阶指南》- 动态规划优化

---

## 动态规划记忆口诀 🎵

### 动态规划基本概念口诀
"重叠子问题，最优子结构，无后效性，记忆化搜索！"

### 动态规划四步法口诀
"定义状态准备食材，状态转移按步骤做，初始状态注意火候，计算顺序按顺序完成！"

### 一维动态规划口诀
"一维数组存状态，依赖前几个状态，空间优化用滚动！"

### 二维动态规划口诀
"二维数组存状态，依赖上左左上状态，空间优化用滚动！"

### 背包问题口诀
"0-1背包从后往前，完全背包从前往后，多重背包三重循环！"

### 区间动态规划口诀
"区间DP按长度，分割点找最优解，回文矩阵链乘法！"

### 状态压缩动态规划口诀
"二进制表示状态，位运算操作状态，旅行商问题经典！"

### 动态规划应用口诀
"爬楼梯打家劫舍，最长递增子序列，背包问题装包游戏，动态规划无处不在！"

---

**记住**: 动态规划就像聪明的记忆大师，记住已经算过的结果，避免重复计算！掌握了动态规划，你就能在编程的世界里游刃有余！加油，未来的动态规划大师！🚀✨
