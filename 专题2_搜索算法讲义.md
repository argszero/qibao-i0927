# 专题2: 搜索算法讲义 🔍
## 初一学生专用版 - 探索未知世界的智慧！

## 课程信息
- **授课老师**: 孔维玲 (东师附中国际金牌教练)
- **课程时间**: 10月3日 08:00-11:30
- **目标学生**: 初一学生，准备冲刺CSP-J高分
- **课程性质**: B层训练营核心课程

---

## 目录
1. [搜索是什么？](#1-搜索是什么)
2. [深度优先搜索 - 一条路走到黑](#2-深度优先搜索---一条路走到黑)
3. [广度优先搜索 - 齐头并进](#3-广度优先搜索---齐头并进)
4. [回溯算法 - 走错了就回头](#4-回溯算法---走错了就回头)
5. [剪枝优化 - 聪明的搜索](#5-剪枝优化---聪明的搜索)
6. [搜索的应用 - 解决实际问题](#6-搜索的应用---解决实际问题)
7. [趣味例题](#7-趣味例题)
8. [挑战练习](#8-挑战练习)

---

## 1. 搜索是什么？

### 1.1 搜索就像探险 🗺️
**核心原理**: 搜索就是在一个"地图"上找路，就像探险家探索未知世界一样！

**简单理解**: 搜索 = 在地图上找路径 + 探索所有可能

**生活例子**: 就像在迷宫里找出口，或者在地图上找从家到学校的最短路线。

### 1.2 搜索的两种策略 🎯
- **深度优先搜索(DFS)**: 像走迷宫，一条路走到黑，走不通再回头
- **广度优先搜索(BFS)**: 像水波扩散，同时向四面八方探索

**记忆口诀**: "深度一条路，广度齐头进！"

### 1.3 搜索的基本要素 🧩
1. **状态**: 当前在哪个位置
2. **转移**: 从当前位置能到哪里
3. **目标**: 要找什么
4. **路径**: 怎么到达目标

**生活例子**: 就像导航，要知道现在在哪(状态)，能走哪些路(转移)，要去哪里(目标)，怎么走(路径)。

---

## 2. 深度优先搜索 - 一条路走到黑 🚶‍♂️

### 2.1 DFS的秘密 - 钻牛角尖的精神 🔍
**核心原理**: 就像走迷宫，选择一条路一直走下去，直到走不通再回头换另一条路！

**简单理解**: 先深入，再回溯，一条路走到黑！

**生活例子**: 就像探索山洞，遇到岔路时选择一条深入探索，走到底再回来探索其他岔路。

### 2.2 DFS的实现方式 📝
**递归实现**: 用函数调用自己
**栈实现**: 用栈来模拟递归过程

```cpp
// 递归版本的DFS
void dfs(int current) {
    if (isTarget(current)) {  // 找到目标了
        return;
    }
    
    visited[current] = true;  // 标记已访问
    
    for (int next : getNeighbors(current)) {  // 遍历所有邻居
        if (!visited[next]) {  // 如果没访问过
            dfs(next);  // 递归访问
        }
    }
}

// 栈版本的DFS
void dfsStack(int start) {
    stack<int> s;
    s.push(start);
    visited[start] = true;
    
    while (!s.empty()) {
        int current = s.top();
        s.pop();
        
        if (isTarget(current)) {  // 找到目标
            return;
        }
        
        for (int next : getNeighbors(current)) {
            if (!visited[next]) {
                visited[next] = true;
                s.push(next);
            }
        }
    }
}
```

### 2.3 DFS的特点 ✨
- **优点**: 内存占用少，容易实现
- **缺点**: 可能走很远的路，不一定找到最短路径
- **适用**: 需要遍历所有可能的情况

**记忆口诀**: "深度优先，一条路走到黑，内存省，路径长！"

---

## 3. 广度优先搜索 - 齐头并进 🌊

### 3.1 BFS的秘密 - 水波扩散的智慧 💧
**核心原理**: 就像水波扩散，从起点开始，同时向四面八方探索，一层一层地推进！

**简单理解**: 齐头并进，一层一层地搜索！

**生活例子**: 就像石头扔进水里，水波一圈一圈地扩散，同时到达所有方向。

### 3.2 BFS的实现方式 📝
**队列实现**: 用队列来保证先进先出

```cpp
void bfs(int start) {
    queue<int> q;
    q.push(start);
    visited[start] = true;
    
    while (!q.empty()) {
        int current = q.front();
        q.pop();
        
        if (isTarget(current)) {  // 找到目标
            return;
        }
        
        for (int next : getNeighbors(current)) {
            if (!visited[next]) {
                visited[next] = true;
                q.push(next);
            }
        }
    }
}

// 带距离的BFS（求最短路径）
void bfsWithDistance(int start) {
    queue<pair<int, int>> q;  // {位置, 距离}
    q.push({start, 0});
    visited[start] = true;
    
    while (!q.empty()) {
        auto [current, dist] = q.front();
        q.pop();
        
        if (isTarget(current)) {
            return dist;  // 返回最短距离
        }
        
        for (int next : getNeighbors(current)) {
            if (!visited[next]) {
                visited[next] = true;
                q.push({next, dist + 1});
            }
        }
    }
    return -1;  // 没找到
}
```

### 3.3 BFS的特点 ✨
- **优点**: 能找到最短路径，层次清晰
- **缺点**: 内存占用大，需要存储所有层
- **适用**: 需要找最短路径或层次遍历

**记忆口诀**: "广度优先，齐头并进，路径短，内存大！"

---

## 4. 回溯算法 - 走错了就回头 🔄

### 4.1 回溯的秘密 - 试错的艺术 🎨
**核心原理**: 就像走迷宫，如果发现走错了路，就回到上一个路口重新选择！

**简单理解**: 尝试所有可能，走错了就回头重来！

**生活例子**: 就像解数独，如果填错了数字，就擦掉重新填。

### 4.2 回溯的经典问题 🎯

#### 4.2.1 全排列问题 - 排队游戏 👥
**问题描述**: 给定n个不同的数字，输出所有可能的排列。

**生活例子**: 就像给3个同学排队，有多少种不同的排队方式？

```cpp
vector<vector<int>> result;
vector<int> path;
vector<bool> used;

void backtrack(vector<int>& nums) {
    if (path.size() == nums.size()) {  // 排完了
        result.push_back(path);
        return;
    }
    
    for (int i = 0; i < nums.size(); i++) {
        if (used[i]) continue;  // 这个数字已经用过了
        
        path.push_back(nums[i]);  // 选择这个数字
        used[i] = true;
        
        backtrack(nums);  // 递归
        
        path.pop_back();  // 撤销选择
        used[i] = false;
    }
}
```

#### 4.2.2 N皇后问题 - 皇后不打架 👑
**问题描述**: 在N×N的棋盘上放置N个皇后，使得它们不能相互攻击。

**生活例子**: 就像在棋盘上放皇后，每个皇后都不能在同一行、列或对角线上。

```cpp
vector<vector<string>> result;
vector<string> board;

bool isValid(int row, int col, int n) {
    // 检查列
    for (int i = 0; i < row; i++) {
        if (board[i][col] == 'Q') return false;
    }
    
    // 检查对角线
    for (int i = row - 1, j = col - 1; i >= 0 && j >= 0; i--, j--) {
        if (board[i][j] == 'Q') return false;
    }
    
    for (int i = row - 1, j = col + 1; i >= 0 && j < n; i--, j++) {
        if (board[i][j] == 'Q') return false;
    }
    
    return true;
}

void backtrack(int row, int n) {
    if (row == n) {  // 所有行都放完了
        result.push_back(board);
        return;
    }
    
    for (int col = 0; col < n; col++) {
        if (isValid(row, col, n)) {  // 这个位置可以放皇后
            board[row][col] = 'Q';  // 放皇后
            backtrack(row + 1, n);  // 递归下一行
            board[row][col] = '.';  // 撤销
        }
    }
}
```

#### 4.2.3 组合问题 - 选朋友游戏 👫
**问题描述**: 从n个数字中选择k个数字，输出所有可能的组合。

**生活例子**: 就像从班级里选3个同学组成小组，有多少种选法？

```cpp
vector<vector<int>> result;
vector<int> path;

void backtrack(int start, int n, int k) {
    if (path.size() == k) {  // 选够了
        result.push_back(path);
        return;
    }
    
    for (int i = start; i <= n; i++) {
        path.push_back(i);  // 选择这个数字
        backtrack(i + 1, n, k);  // 递归选择下一个
        path.pop_back();  // 撤销选择
    }
}
```

### 4.3 回溯的模板 📋
```cpp
void backtrack(参数) {
    if (终止条件) {
        收集结果;
        return;
    }
    
    for (选择 : 本层集合中元素) {
        处理节点;
        backtrack(路径, 选择列表);  // 递归
        回溯, 撤销处理结果;
    }
}
```

**回溯算法记忆口诀**: "选择递归撤销，试错的艺术！"

---

## 5. 剪枝优化 - 聪明的搜索 ✂️

### 5.1 剪枝的秘密 - 提前放弃的智慧 🧠
**核心原理**: 就像修剪树枝，把不可能有结果的路径提前剪掉，节省时间！

**简单理解**: 发现走不通就提前回头，不要浪费时间！

**生活例子**: 就像找东西，如果知道某个房间肯定没有，就不要进去找了。

### 5.2 常见的剪枝策略 🌿

#### 5.2.1 可行性剪枝 - 提前判断
```cpp
void backtrack(vector<int>& nums, int target, int start, int sum) {
    if (sum == target) {  // 找到解
        result.push_back(path);
        return;
    }
    
    if (sum > target) return;  // 可行性剪枝：和已经超过目标了
    
    for (int i = start; i < nums.size(); i++) {
        if (i > start && nums[i] == nums[i-1]) continue;  // 去重剪枝
        
        path.push_back(nums[i]);
        backtrack(nums, target, i + 1, sum + nums[i]);
        path.pop_back();
    }
}
```

#### 5.2.2 最优性剪枝 - 提前放弃
```cpp
void backtrack(int current, int cost) {
    if (cost >= bestCost) return;  // 最优性剪枝：当前成本已经不比最优解好了
    
    if (isComplete()) {
        bestCost = min(bestCost, cost);
        return;
    }
    
    for (int next : getNextOptions()) {
        backtrack(next, cost + getCost(current, next));
    }
}
```

#### 5.2.3 搜索顺序优化 - 先试好的
```cpp
void backtrack(vector<int>& nums) {
    if (isComplete()) {
        result.push_back(path);
        return;
    }
    
    // 按某种顺序排序，先试更有可能的选择
    sort(nums.begin(), nums.end(), compare);
    
    for (int num : nums) {
        if (isValid(num)) {
            path.push_back(num);
            backtrack(nums);
            path.pop_back();
        }
    }
}
```

### 5.3 剪枝的效果 ⚡
- **时间优化**: 避免搜索无用的路径
- **空间优化**: 减少递归深度
- **效率提升**: 大幅提高搜索速度

**剪枝记忆口诀**: "提前判断，提前放弃，搜索更聪明！"

---

## 6. 搜索的应用 - 解决实际问题 🛠️

### 6.1 迷宫问题 - 找出口游戏 🏰
**问题描述**: 在迷宫中从起点找到出口的最短路径。

**生活例子**: 就像在游戏里走迷宫，要找到出口。

```cpp
int dx[] = {-1, 1, 0, 0};  // 上下左右
int dy[] = {0, 0, -1, 1};

int bfsMaze(vector<vector<int>>& maze, int startX, int startY, int endX, int endY) {
    queue<pair<pair<int, int>, int>> q;  // {{x, y}, 距离}
    q.push({{startX, startY}, 0});
    maze[startX][startY] = 1;  // 标记为已访问
    
    while (!q.empty()) {
        auto [pos, dist] = q.front();
        auto [x, y] = pos;
        q.pop();
        
        if (x == endX && y == endY) {  // 找到出口
            return dist;
        }
        
        for (int i = 0; i < 4; i++) {  // 四个方向
            int nx = x + dx[i];
            int ny = y + dy[i];
            
            if (nx >= 0 && nx < maze.size() && ny >= 0 && ny < maze[0].size() 
                && maze[nx][ny] == 0) {  // 可以走
                maze[nx][ny] = 1;  // 标记已访问
                q.push({{nx, ny}, dist + 1});
            }
        }
    }
    
    return -1;  // 没找到出口
}
```

### 6.2 岛屿问题 - 数岛屿游戏 🏝️
**问题描述**: 计算二维网格中岛屿的数量。

**生活例子**: 就像在地图上数有多少个岛屿。

```cpp
void dfsIsland(vector<vector<char>>& grid, int x, int y) {
    if (x < 0 || x >= grid.size() || y < 0 || y >= grid[0].size() 
        || grid[x][y] != '1') {
        return;  // 越界或不是陆地
    }
    
    grid[x][y] = '0';  // 标记为已访问
    
    // 四个方向递归
    dfsIsland(grid, x + 1, y);
    dfsIsland(grid, x - 1, y);
    dfsIsland(grid, x, y + 1);
    dfsIsland(grid, x, y - 1);
}

int numIslands(vector<vector<char>>& grid) {
    int count = 0;
    
    for (int i = 0; i < grid.size(); i++) {
        for (int j = 0; j < grid[0].size(); j++) {
            if (grid[i][j] == '1') {  // 发现新岛屿
                dfsIsland(grid, i, j);  // 标记整个岛屿
                count++;
            }
        }
    }
    
    return count;
}
```

### 6.3 单词搜索 - 找单词游戏 📝
**问题描述**: 在二维网格中搜索单词，可以上下左右移动。

**生活例子**: 就像在字母网格中找单词，像填字游戏。

```cpp
bool dfsWord(vector<vector<char>>& board, string& word, int x, int y, int index) {
    if (index == word.length()) return true;  // 找到完整单词
    
    if (x < 0 || x >= board.size() || y < 0 || y >= board[0].size() 
        || board[x][y] != word[index]) {
        return false;  // 越界或字符不匹配
    }
    
    char temp = board[x][y];
    board[x][y] = '#';  // 标记已访问
    
    // 四个方向搜索
    bool found = dfsWord(board, word, x + 1, y, index + 1) ||
                 dfsWord(board, word, x - 1, y, index + 1) ||
                 dfsWord(board, word, x, y + 1, index + 1) ||
                 dfsWord(board, word, x, y - 1, index + 1);
    
    board[x][y] = temp;  // 恢复
    return found;
}

bool exist(vector<vector<char>>& board, string word) {
    for (int i = 0; i < board.size(); i++) {
        for (int j = 0; j < board[0].size(); j++) {
            if (dfsWord(board, word, i, j, 0)) {
                return true;
            }
        }
    }
    return false;
}
```

### 6.4 搜索的应用总结 🎯
- **图遍历**: 访问图中的所有节点
- **路径查找**: 找最短路径或所有路径
- **组合优化**: 找最优解或所有解
- **状态空间搜索**: 解决复杂的状态转换问题

**应用记忆口诀**: "图遍历，路径找，组合优化状态搜！"

---

## 7. 趣味例题 🎮

### 例题1: 二叉树的最大深度 - 数楼层游戏 🏢
**题目**: 给定一个二叉树，找出其最大深度。

**生活例子**: 就像数一栋楼有多少层，从最底层数到最顶层。

```cpp
struct TreeNode {
    int val;
    TreeNode* left;
    TreeNode* right;
};

// DFS解法
int maxDepth(TreeNode* root) {
    if (root == nullptr) return 0;  // 空树深度为0
    
    int leftDepth = maxDepth(root->left);   // 左子树深度
    int rightDepth = maxDepth(root->right); // 右子树深度
    
    return max(leftDepth, rightDepth) + 1;  // 当前层 + 子树最大深度
}

// BFS解法
int maxDepthBFS(TreeNode* root) {
    if (root == nullptr) return 0;
    
    queue<TreeNode*> q;
    q.push(root);
    int depth = 0;
    
    while (!q.empty()) {
        int size = q.size();  // 当前层的节点数
        depth++;
        
        for (int i = 0; i < size; i++) {
            TreeNode* node = q.front();
            q.pop();
            
            if (node->left) q.push(node->left);
            if (node->right) q.push(node->right);
        }
    }
    
    return depth;
}
```

### 例题2: 对称二叉树 - 照镜子游戏 🪞
**题目**: 判断一个二叉树是否是对称的。

**生活例子**: 就像照镜子，左右两边要完全一样。

```cpp
bool isSymmetric(TreeNode* root) {
    if (root == nullptr) return true;
    return isMirror(root->left, root->right);
}

bool isMirror(TreeNode* left, TreeNode* right) {
    if (left == nullptr && right == nullptr) return true;  // 都为空
    if (left == nullptr || right == nullptr) return false; // 一个为空
    
    return left->val == right->val &&  // 值相等
           isMirror(left->left, right->right) &&  // 左左对右右
           isMirror(left->right, right->left);    // 左右对右左
}
```

### 例题3: 路径总和 - 找数字游戏 🔢
**题目**: 判断是否存在从根节点到叶子节点的路径，使得路径上所有节点值的和等于目标值。

**生活例子**: 就像在树上找一条路，路上的数字加起来等于目标数字。

```cpp
bool hasPathSum(TreeNode* root, int targetSum) {
    if (root == nullptr) return false;  // 空树
    
    if (root->left == nullptr && root->right == nullptr) {  // 叶子节点
        return root->val == targetSum;  // 检查是否等于目标值
    }
    
    int newTarget = targetSum - root->val;  // 新的目标值
    
    return hasPathSum(root->left, newTarget) ||  // 左子树
           hasPathSum(root->right, newTarget);   // 右子树
}
```

### 例题4: 全排列II - 去重排队游戏 👥
**题目**: 给定可包含重复数字的序列，返回所有不重复的全排列。

**生活例子**: 就像给有重复名字的同学排队，但相同的排列只算一种。

```cpp
vector<vector<int>> permuteUnique(vector<int>& nums) {
    vector<vector<int>> result;
    vector<int> path;
    vector<bool> used(nums.size(), false);
    
    sort(nums.begin(), nums.end());  // 排序，方便去重
    
    backtrack(nums, path, used, result);
    return result;
}

void backtrack(vector<int>& nums, vector<int>& path, vector<bool>& used, 
               vector<vector<int>>& result) {
    if (path.size() == nums.size()) {
        result.push_back(path);
        return;
    }
    
    for (int i = 0; i < nums.size(); i++) {
        if (used[i]) continue;  // 已使用
        
        // 去重：如果当前数字和前一个数字相同，且前一个数字未使用，则跳过
        if (i > 0 && nums[i] == nums[i-1] && !used[i-1]) continue;
        
        path.push_back(nums[i]);
        used[i] = true;
        
        backtrack(nums, path, used, result);
        
        path.pop_back();
        used[i] = false;
    }
}
```

**例题总结**:
- 二叉树深度：数楼层，DFS或BFS 🏢
- 对称二叉树：照镜子，递归比较 🪞
- 路径总和：找数字，递归搜索 🔢
- 全排列II：去重排队，回溯+剪枝 👥

---

## 8. 挑战练习 🏆

### 基础练习 - 新手村任务 🎯
1. **DFS练习 - 深度探索者**
   - 二叉树的前序遍历
   - 二叉树的中序遍历
   - 二叉树的后序遍历
   - 二叉树的最大深度

2. **BFS练习 - 广度探索者**
   - 二叉树的层序遍历
   - 二叉树的最小深度
   - 二叉树的右视图
   - 二叉树的左视图

3. **回溯练习 - 试错大师**
   - 全排列问题
   - 组合问题
   - 子集问题
   - 电话号码的字母组合

### 进阶练习 - 冒险者任务 ⚔️
1. **图搜索 - 网络探索者**
   - 岛屿数量
   - 被围绕的区域
   - 单词搜索
   - 课程表

2. **路径搜索 - 导航专家**
   - 最短路径
   - 所有路径
   - 路径总和
   - 路径总和II

3. **状态搜索 - 状态转换师**
   - 八皇后问题
   - 数独求解
   - 解数独
   - 单词接龙

### 高级练习 - 王者挑战 👑
1. **复杂搜索 - 搜索大师**
   - 单词接龙II
   - 最小基因变化
   - 打开转盘锁
   - 滑动谜题

2. **优化搜索 - 效率专家**
   - 带剪枝的回溯
   - 记忆化搜索
   - 双向BFS
   - A*搜索

3. **综合应用 - 终极挑战**
   - 迷宫问题
   - 骑士周游问题
   - 旅行商问题
   - 数独求解器

---

## 学习建议 - 成为搜索高手 🎓

### 1. 理解搜索本质 - 掌握核心思想 🧠
- **状态空间**: 理解什么是状态，如何表示状态
- **状态转移**: 理解如何从一个状态到另一个状态
- **搜索策略**: 理解DFS和BFS的区别和适用场景
- **回溯思想**: 理解试错和撤销的思想

### 2. 掌握实现技巧 - 熟练编程 💻
- **递归实现**: 掌握递归的写法和终止条件
- **迭代实现**: 掌握用栈和队列实现搜索
- **状态标记**: 掌握如何标记已访问的状态
- **路径记录**: 掌握如何记录搜索路径

### 3. 学会优化方法 - 提高效率 ⚡
- **剪枝技巧**: 学会提前判断和放弃
- **记忆化**: 学会避免重复计算
- **搜索顺序**: 学会选择更好的搜索顺序
- **数据结构**: 学会选择合适的数据结构

### 4. 大量练习 - 熟能生巧 🏃‍♂️
- **从简单开始**: 先做基础的遍历问题
- **逐步提高**: 再做复杂的搜索问题
- **总结模板**: 记住常用的搜索模板
- **分析复杂度**: 理解时间和空间复杂度

---

## 课后作业 - 巩固练习 📋

### 必做作业（基础）
1. **DFS基础练习**
   - 实现二叉树的三种遍历
   - 计算二叉树的最大深度
   - 判断二叉树是否对称
   - 实现岛屿数量问题

2. **BFS基础练习**
   - 实现二叉树的层序遍历
   - 计算二叉树的最小深度
   - 实现单词搜索问题
   - 解决迷宫最短路径

3. **回溯基础练习**
   - 实现全排列问题
   - 解决N皇后问题
   - 实现组合问题
   - 解决子集问题

### 选做作业（进阶）
4. **图搜索练习**
   - 实现图的DFS和BFS遍历
   - 解决课程表问题
   - 实现单词接龙
   - 解决被围绕的区域

5. **路径搜索练习**
   - 实现所有路径问题
   - 解决路径总和问题
   - 实现最短路径算法
   - 解决路径总和II

### 挑战作业（高手）
6. **综合应用**
   - 实现数独求解器
   - 解决八皇后问题
   - 实现A*搜索算法
   - 挑战旅行商问题

---

## 学习进度检查 ✅

### 第一周目标
- [ ] 掌握DFS和BFS的基本思想
- [ ] 实现二叉树的遍历算法
- [ ] 理解回溯算法的思想
- [ ] 完成基础搜索练习

### 第二周目标
- [ ] 掌握图搜索算法
- [ ] 实现路径搜索问题
- [ ] 学会剪枝优化技巧
- [ ] 完成进阶搜索练习

### 第三周目标
- [ ] 掌握复杂搜索问题
- [ ] 实现搜索优化算法
- [ ] 解决综合应用问题
- [ ] 挑战高级搜索题目

---

## 学习资源推荐 📖

### 在线练习平台
- **LeetCode**: 搜索算法专题练习
- **洛谷**: 适合初学者的搜索题目
- **Codeforces**: 竞赛级别的搜索问题
- **AtCoder**: 高质量的搜索算法题

### 学习网站
- **算法导论**: 深度优先搜索和广度优先搜索
- **数据结构与算法分析**: 图算法和搜索
- **算法竞赛入门经典**: 搜索算法实战
- **算法竞赛进阶指南**: 高级搜索技巧

### 推荐书籍
- 《算法导论》- 图算法章节
- 《数据结构与算法分析》- 图论章节
- 《算法竞赛入门经典》- 搜索算法
- 《算法竞赛进阶指南》- 搜索优化

---

## 搜索算法记忆口诀 🎵

### DFS记忆口诀
"深度优先一条路，递归栈来都实现，内存省但路径长，遍历所有最擅长！"

### BFS记忆口诀
"广度优先齐头进，队列实现最标准，路径短但内存大，最短路径它最棒！"

### 回溯记忆口诀
"选择递归再撤销，试错艺术要记牢，剪枝优化效率高，组合排列都能搞！"

### 搜索应用口诀
"图遍历，路径找，组合优化状态搜，迷宫岛屿单词找，搜索算法真奇妙！"

---

**记住**: 搜索算法就像探险，需要勇气去探索未知，也需要智慧去选择路径！掌握了搜索，你就能在编程的世界里自由探索！加油，未来的搜索大师！🚀✨
