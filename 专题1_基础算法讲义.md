# 专题1: 基础算法讲义 🚀
## 初一学生专用版 - 让算法变得简单有趣！

## 课程信息
- **授课老师**: 王昌平
- **课程时间**: 10月2日 13:30-16:30
- **目标学生**: 初一学生，准备冲刺CSP-J高分
- **课程性质**: B层训练营核心课程

---

## 目录
1. [算法是什么？](#1-算法是什么)
2. [排序算法 - 整理房间的艺术](#2-排序算法---整理房间的艺术)
3. [查找算法 - 找东西的智慧](#3-查找算法---找东西的智慧)
4. [贪心算法 - 做选择的小聪明](#4-贪心算法---做选择的小聪明)
5. [模拟算法 - 按部就班的执行](#5-模拟算法---按部就班的执行)
6. [递归与分治 - 大事化小的智慧](#6-递归与分治---大事化小的智慧)
7. [趣味例题](#7-趣味例题)
8. [挑战练习](#8-挑战练习)

---

## 1. 算法是什么？

### 1.1 算法就像做菜的食谱 🍳
**核心原理**: 算法就是解决问题的"食谱"，告诉计算机先做什么，后做什么，就像妈妈做菜时先洗菜、再切菜、最后炒菜一样！

**简单理解**: 算法 = 步骤清单 + 明确指令

### 1.2 好算法的五个特点 ✨
- **有输入**: 就像做菜需要食材
- **有输出**: 就像做菜要做出美味佳肴
- **步骤清楚**: 每一步都要说得明明白白
- **能结束**: 不能永远做不完
- **真的有用**: 每一步都要能执行

### 1.3 算法快慢的秘密 ⏰
**时间复杂度**: 就像跑步，有的算法像蜗牛🐌，有的像兔子🐰

- **O(1)**: 瞬间完成，像按开关一样快
- **O(log n)**: 很快，像翻字典找字
- **O(n)**: 一般快，像数人数，一个接一个
- **O(n²)**: 比较慢，像检查每两个人是否认识
- **O(2ⁿ)**: 超级慢，像尝试所有可能的密码组合

**记忆口诀**: "常数对数线性好，平方指数要逃跑！"

---

## 2. 排序算法 - 整理房间的艺术 🏠

### 2.1 冒泡排序 - 像气泡一样往上冒 🫧
**核心原理**: 就像气泡在水里往上冒一样，大的数字会"冒"到后面去！

**生活例子**: 整理书架时，把大的书一本本往后推，轻的书自然就到前面了。

```cpp
void bubbleSort(int arr[], int n) {
    for (int i = 0; i < n-1; i++) {
        for (int j = 0; j < n-i-1; j++) {
            if (arr[j] > arr[j+1]) {
                swap(arr[j], arr[j+1]);  // 大的往后"冒"
            }
        }
    }
}
```
**特点**: 简单易懂，但比较慢 🐌

### 2.2 选择排序 - 每次都选最小的 🎯
**核心原理**: 就像选班干部，每次都从剩下的人里选最优秀的！

**生活例子**: 整理扑克牌时，每次都找出最小的牌放到最前面。

```cpp
void selectionSort(int arr[], int n) {
    for (int i = 0; i < n-1; i++) {
        int minIdx = i;  // 假设当前位置是最小的
        for (int j = i+1; j < n; j++) {
            if (arr[j] < arr[minIdx]) {
                minIdx = j;  // 找到更小的了！
            }
        }
        swap(arr[i], arr[minIdx]);  // 把最小的放到前面
    }
}
```
**特点**: 每次都能确定一个位置，但还是要比较很多次

### 2.3 插入排序 - 像插队一样 📚
**核心原理**: 就像排队时插队，把新来的人放到合适的位置！

**生活例子**: 整理扑克牌时，每拿到一张新牌就插入到已经排好的牌中。

```cpp
void insertionSort(int arr[], int n) {
    for (int i = 1; i < n; i++) {
        int key = arr[i];  // 要插入的"新牌"
        int j = i - 1;
        while (j >= 0 && arr[j] > key) {
            arr[j+1] = arr[j];  // 把大的牌往后挪
            j--;
        }
        arr[j+1] = key;  // 插入到合适位置
    }
}
```
**特点**: 对于已经基本有序的数据特别快！

### 2.4 快速排序 - 分而治之的高手 ⚡
**核心原理**: 就像排队，先选一个"标杆"，比标杆高的站右边，低的站左边，然后分别排序！

**生活例子**: 整理房间时，先按颜色分类，再分别整理每个颜色的物品。

```cpp
int partition(int arr[], int low, int high) {
    int pivot = arr[high];  // 选最后一个做"标杆"
    int i = low - 1;
    
    for (int j = low; j < high; j++) {
        if (arr[j] <= pivot) {
            i++;
            swap(arr[i], arr[j]);  // 小的放左边
        }
    }
    swap(arr[i+1], arr[high]);  // 标杆放到中间
    return i + 1;
}

void quickSort(int arr[], int low, int high) {
    if (low < high) {
        int pi = partition(arr, low, high);  // 分界点
        quickSort(arr, low, pi - 1);         // 排序左边
        quickSort(arr, pi + 1, high);        // 排序右边
    }
}
```
**特点**: 平均情况下很快，但最坏情况比较慢

### 2.5 归并排序 - 合并高手的绝招 🤝
**核心原理**: 就像合并两个已经排好序的队伍，每次比较队首，把小的先放出来！

**生活例子**: 整理两摞已经按大小排好的书，每次比较两摞最上面的书，把小的放到新摞里。

```cpp
void merge(int arr[], int left, int mid, int right) {
    // 创建两个临时数组
    int n1 = mid - left + 1;
    int n2 = right - mid;
    int L[n1], R[n2];
    
    // 复制数据到临时数组
    for (int i = 0; i < n1; i++) L[i] = arr[left + i];
    for (int j = 0; j < n2; j++) R[j] = arr[mid + 1 + j];
    
    int i = 0, j = 0, k = left;
    
    // 合并两个数组
    while (i < n1 && j < n2) {
        if (L[i] <= R[j]) {
            arr[k] = L[i];  // 左边的小，先放
            i++;
        } else {
            arr[k] = R[j];  // 右边的小，先放
            j++;
        }
        k++;
    }
    
    // 把剩下的元素放完
    while (i < n1) { arr[k] = L[i]; i++; k++; }
    while (j < n2) { arr[k] = R[j]; j++; k++; }
}

void mergeSort(int arr[], int left, int right) {
    if (left < right) {
        int mid = left + (right - left) / 2;
        mergeSort(arr, left, mid);      // 排序左半部分
        mergeSort(arr, mid + 1, right); // 排序右半部分
        merge(arr, left, mid, right);   // 合并两部分
    }
}
```
**特点**: 总是很快，但需要额外空间

**排序算法记忆口诀**:
- 冒泡：气泡往上冒 🫧
- 选择：每次都选最小的 🎯  
- 插入：像插队一样 📚
- 快速：分而治之 ⚡
- 归并：合并高手 🤝

---

## 3. 查找算法 - 找东西的智慧 🔍

### 3.1 线性查找 - 一个一个地找 👀
**核心原理**: 就像在书包里找橡皮，一个一个地翻，直到找到为止！

**生活例子**: 在电话本里找电话号码，从第一页开始一页一页地翻。

```cpp
int linearSearch(int arr[], int n, int target) {
    for (int i = 0; i < n; i++) {
        if (arr[i] == target) {
            return i;  // 找到了！返回位置
        }
    }
    return -1; // 没找到
}
```
**特点**: 简单直接，但比较慢，最坏情况要翻遍所有东西

### 3.2 二分查找 - 猜数字游戏的高手 🎯
**核心原理**: 就像猜数字游戏，每次猜中间的数字，根据大小决定往左还是往右找！

**生活例子**: 在字典里找单词，先翻到中间，看要找的单词在左半部分还是右半部分。

```cpp
int binarySearch(int arr[], int left, int right, int target) {
    while (left <= right) {
        int mid = left + (right - left) / 2;  // 猜中间的数字
        
        if (arr[mid] == target) {
            return mid;  // 找到了！
        }
        
        if (arr[mid] < target) {
            left = mid + 1;   // 目标在右边，往右找
        } else {
            right = mid - 1;  // 目标在左边，往左找
        }
    }
    return -1; // 没找到
}
```
**特点**: 超级快！但要求数据必须排好序

**二分查找记忆口诀**: "中间猜，比大小，左一半，右一半，继续猜！"

### 3.3 查找重复数字的位置 🔢
**核心原理**: 当有重复数字时，我们要找第一个或最后一个出现的位置！

```cpp
// 找第一个等于target的位置
int findFirst(int arr[], int n, int target) {
    int left = 0, right = n - 1;
    int result = -1;
    
    while (left <= right) {
        int mid = left + (right - left) / 2;
        if (arr[mid] == target) {
            result = mid;        // 找到了，但还要继续找
            right = mid - 1;     // 在左边继续找第一个
        } else if (arr[mid] < target) {
            left = mid + 1;
        } else {
            right = mid - 1;
        }
    }
    return result;
}

// 找最后一个等于target的位置
int findLast(int arr[], int n, int target) {
    int left = 0, right = n - 1;
    int result = -1;
    
    while (left <= right) {
        int mid = left + (right - left) / 2;
        if (arr[mid] == target) {
            result = mid;        // 找到了，但还要继续找
            left = mid + 1;      // 在右边继续找最后一个
        } else if (arr[mid] < target) {
            left = mid + 1;
        } else {
            right = mid - 1;
        }
    }
    return result;
}
```

**查找算法总结**:
- 线性查找：像翻书包，简单但慢 👀
- 二分查找：像猜数字，快但要有序 🎯

---

## 4. 贪心算法 - 做选择的小聪明 🧠

### 4.1 贪心算法的秘密 🤔
**核心原理**: 就像吃自助餐，每次都拿最好吃的，希望最后吃得最满足！

**简单理解**: 每一步都选择当前看起来最好的，不回头看，不后悔！

### 4.2 贪心算法的特点 ✨
- **只看眼前**: 每步都选当前最好的
- **不后悔**: 做了选择就不回头
- **希望最好**: 希望局部最优能带来全局最优

**生活例子**: 选课表时，每次都选最喜欢的课，希望最后课表最满意。

### 4.3 经典贪心问题

#### 4.3.1 活动选择问题 - 安排最多活动 🎯
**问题描述**: 有很多活动，每个活动有开始和结束时间，怎么安排能参加最多活动？

**贪心策略**: 每次都选结束时间最早的活动！

**生活例子**: 就像安排一天的活动，每次都选最早结束的，这样能安排更多活动。

```cpp
struct Activity {
    int start, end;
};

bool compare(Activity a, Activity b) {
    return a.end < b.end;  // 按结束时间排序
}

int activitySelection(Activity activities[], int n) {
    sort(activities, activities + n, compare);
    
    int count = 1;
    int lastEnd = activities[0].end;  // 第一个活动
    
    for (int i = 1; i < n; i++) {
        if (activities[i].start >= lastEnd) {  // 不冲突
            count++;
            lastEnd = activities[i].end;
        }
    }
    
    return count;
}
```

#### 4.3.2 找零钱问题 - 用最少硬币 💰
**问题描述**: 有不同面额的硬币，怎么用最少的硬币凑出指定金额？

**贪心策略**: 每次都选面额最大的硬币！

**生活例子**: 就像买东西找零，每次都找面额最大的钱，希望硬币数量最少。

```cpp
int coinChange(vector<int>& coins, int amount) {
    sort(coins.begin(), coins.end(), greater<int>());  // 从大到小排序
    
    int count = 0;
    for (int coin : coins) {
        count += amount / coin;  // 能用几个这种硬币
        amount %= coin;          // 剩下的钱
    }
    
    return amount == 0 ? count : -1;  // 能找零就返回硬币数
}
```

#### 4.3.3 跳跃游戏 - 能跳多远？ 🦘
**问题描述**: 数组中的数字表示能跳的最大步数，问能否跳到最后一个位置？

**贪心策略**: 每次都跳到能跳得最远的位置！

```cpp
bool canJump(vector<int>& nums) {
    int maxReach = 0;  // 当前能到达的最远位置
    
    for (int i = 0; i < nums.size(); i++) {
        if (i > maxReach) return false;  // 到不了当前位置
        maxReach = max(maxReach, i + nums[i]);  // 更新能到达的最远位置
    }
    
    return true;
}
```

**贪心算法记忆口诀**: "眼前最好，不后悔，希望全局最优！"

---

## 5. 模拟算法 - 按部就班的执行 📋

### 5.1 模拟算法的秘密 🤖
**核心原理**: 就像按照说明书组装玩具，题目怎么说，我们就怎么做！

**简单理解**: 不耍小聪明，老老实实按照题目的要求一步步执行。

### 5.2 模拟算法的特点 ✨
- **听话**: 题目怎么说就怎么做
- **仔细**: 不能漏掉任何细节
- **完整**: 要处理所有可能的情况

**生活例子**: 就像做实验，按照实验步骤一步一步来，不能跳过任何步骤。

### 5.3 模拟算法五步法 📝
1. **读题**: 理解题目要我们做什么
2. **设计**: 想好用什么数据结构
3. **实现**: 按照题目要求写代码
4. **检查**: 处理各种特殊情况
5. **测试**: 验证答案是否正确

### 5.4 经典模拟问题

#### 5.4.1 约瑟夫环问题 - 数数游戏 🎲
**问题描述**: n个人围成一圈，从第k个人开始报数，数到m的人出圈，最后剩下谁？

**生活例子**: 就像玩"数到几就淘汰"的游戏，大家围成一圈数数。

```cpp
int josephus(int n, int k) {
    if (n == 1) return 0;  // 只剩一个人，就是他
    return (josephus(n - 1, k) + k) % n;  // 递归计算
}

// 非递归版本（更容易理解）
int josephusIterative(int n, int k) {
    int result = 0;
    for (int i = 2; i <= n; i++) {
        result = (result + k) % i;  // 从2个人开始，逐步计算
    }
    return result;
}
```

#### 5.4.2 螺旋矩阵 - 画螺旋线 🌀
**问题描述**: 按螺旋顺序填充n×n矩阵，从外到内一圈一圈地填数字。

**生活例子**: 就像画螺旋线，从外圈开始，顺时针一圈一圈往里画。

```cpp
vector<vector<int>> generateMatrix(int n) {
    vector<vector<int>> matrix(n, vector<int>(n));
    
    int top = 0, bottom = n - 1, left = 0, right = n - 1;
    int num = 1;
    
    while (top <= bottom && left <= right) {
        // 从左到右画上边
        for (int i = left; i <= right; i++) {
            matrix[top][i] = num++;
        }
        top++;  // 上边画完了，往下移
        
        // 从上到下画右边
        for (int i = top; i <= bottom; i++) {
            matrix[i][right] = num++;
        }
        right--;  // 右边画完了，往左移
        
        // 从右到左画下边（如果还有的话）
        if (top <= bottom) {
            for (int i = right; i >= left; i--) {
                matrix[bottom][i] = num++;
            }
            bottom--;  // 下边画完了，往上移
        }
        
        // 从下到上画左边（如果还有的话）
        if (left <= right) {
            for (int i = bottom; i >= top; i--) {
                matrix[i][left] = num++;
            }
            left++;  // 左边画完了，往右移
        }
    }
    
    return matrix;
}
```

#### 5.4.3 旋转图像 - 转魔方 🎯
**问题描述**: 将n×n矩阵顺时针旋转90度。

**生活例子**: 就像转魔方，把整个图案顺时针转90度。

```cpp
void rotate(vector<vector<int>>& matrix) {
    int n = matrix.size();
    
    // 先沿对角线翻转
    for (int i = 0; i < n; i++) {
        for (int j = i; j < n; j++) {
            swap(matrix[i][j], matrix[j][i]);
        }
    }
    
    // 再沿中轴线翻转
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n/2; j++) {
            swap(matrix[i][j], matrix[i][n-1-j]);
        }
    }
}
```

**模拟算法记忆口诀**: "题目怎么说，我就怎么做，一步一步不偷懒！"

---

## 6. 递归与分治 - 大事化小的智慧 🧩

### 6.1 递归的秘密 - 自己调用自己 🔄
**核心原理**: 就像俄罗斯套娃，大问题里面套着小问题，小问题和大问题长得一样！

**简单理解**: 遇到大问题，就把它分成小问题，小问题用同样的方法解决。

**生活例子**: 就像数人数，要数一个班的人数，就让每个小组先数自己组的人数，然后加起来。

### 6.2 递归三要素 - 递归的规则 📋
1. **停止条件**: 什么时候不再递归了（就像套娃最小的那个）
2. **递归关系**: 大问题和小问题的关系
3. **递归调用**: 调用自己解决小问题

**记忆口诀**: "有停止，有关系，要调用！"

### 6.3 经典递归问题

#### 6.3.1 斐波那契数列 - 兔子繁殖问题 🐰
**问题描述**: 斐波那契数列：1, 1, 2, 3, 5, 8, 13... 每个数都是前两个数的和。

**生活例子**: 就像兔子繁殖，第一年有1对兔子，第二年还是1对，第三年开始每年都是前两年的总和。

```cpp
// 递归版本（容易理解但很慢）
int fibonacci(int n) {
    if (n <= 1) return n;  // 停止条件
    return fibonacci(n - 1) + fibonacci(n - 2);  // 递归关系
}

// 记忆化版本（快一些）
int memo[1000];
int fibonacciMemo(int n) {
    if (n <= 1) return n;
    if (memo[n] != -1) return memo[n];  // 已经算过了
    return memo[n] = fibonacciMemo(n - 1) + fibonacciMemo(n - 2);
}

// 动态规划版本（最快）
int fibonacciDP(int n) {
    if (n <= 1) return n;
    int a = 0, b = 1;
    for (int i = 2; i <= n; i++) {
        int temp = a + b;
        a = b;
        b = temp;
    }
    return b;
}
```

#### 6.3.2 汉诺塔问题 - 搬盘子游戏 🎯
**问题描述**: 有三根柱子，把盘子从一根柱子搬到另一根柱子，每次只能搬一个，大盘子不能放在小盘子上面。

**生活例子**: 就像搬盘子，要把一摞盘子从A搬到C，需要借助B，每次只能搬最上面的一个。

```cpp
void hanoi(int n, char from, char to, char aux) {
    if (n == 1) {  // 停止条件：只剩一个盘子
        cout << "Move disk 1 from " << from << " to " << to << endl;
        return;
    }
    
    // 递归关系：分三步
    hanoi(n - 1, from, aux, to);  // 1. 把上面n-1个盘子搬到辅助柱子
    cout << "Move disk " << n << " from " << from << " to " << to << endl;  // 2. 把最大的盘子搬到目标柱子
    hanoi(n - 1, aux, to, from);  // 3. 把n-1个盘子从辅助柱子搬到目标柱子
}
```

### 6.4 分治算法 - 分而治之的智慧 ⚔️
**核心原理**: 就像打仗，把大部队分成小部队，分别作战，最后合并战果！

**简单理解**: 把大问题分成小问题，分别解决，再把结果合并。

#### 6.4.1 快速幂 - 快速计算幂次 🚀
**问题描述**: 计算a的b次方，要求很快。

**生活例子**: 就像计算2的100次方，直接乘100次太慢，可以分而治之。

```cpp
long long power(long long base, long long exp, long long mod) {
    if (exp == 0) return 1;  // 停止条件：任何数的0次方都是1
    if (exp == 1) return base % mod;  // 停止条件：任何数的1次方都是自己
    
    long long half = power(base, exp / 2, mod);  // 分治：计算一半
    half = (half * half) % mod;  // 合并：平方
    
    if (exp % 2 == 1) {  // 如果指数是奇数
        half = (half * base) % mod;  // 再乘一次底数
    }
    
    return half;
}
```

#### 6.4.2 最大子数组和 - 找最赚钱的连续几天 💰
**问题描述**: 给定数组，找连续子数组的最大和。

**生活例子**: 就像股票，找连续几天中赚钱最多的那几天。

```cpp
int maxSubArray(vector<int>& nums) {
    return maxSubArrayHelper(nums, 0, nums.size() - 1);
}

int maxSubArrayHelper(vector<int>& nums, int left, int right) {
    if (left == right) return nums[left];  // 停止条件：只有一个元素
    
    int mid = left + (right - left) / 2;
    
    // 分治：分别求左边、右边、跨中点的最大值
    int leftMax = maxSubArrayHelper(nums, left, mid);
    int rightMax = maxSubArrayHelper(nums, mid + 1, right);
    int crossMax = maxCrossingSum(nums, left, mid, right);
    
    return max({leftMax, rightMax, crossMax});  // 合并：取三者最大值
}

int maxCrossingSum(vector<int>& nums, int left, int mid, int right) {
    // 从mid向左找最大和
    int leftSum = INT_MIN, sum = 0;
    for (int i = mid; i >= left; i--) {
        sum += nums[i];
        leftSum = max(leftSum, sum);
    }
    
    // 从mid+1向右找最大和
    int rightSum = INT_MIN;
    sum = 0;
    for (int i = mid + 1; i <= right; i++) {
        sum += nums[i];
        rightSum = max(rightSum, sum);
    }
    
    return leftSum + rightSum;  // 合并：跨中点的最大和
}
```

**递归与分治记忆口诀**: "大事化小，小事化了，分而治之，合而为一！"

---

## 7. 趣味例题 🎮

### 例题1: 两数之和 - 找朋友游戏 👫
**题目**: 给定一个整数数组和一个目标值，找出数组中和为目标值的两个数的索引。

**生活例子**: 就像找朋友，要找到两个数字"牵手"后等于目标值！

**解法1: 暴力枚举 - 一个一个试**
```cpp
vector<int> twoSum(vector<int>& nums, int target) {
    for (int i = 0; i < nums.size(); i++) {
        for (int j = i + 1; j < nums.size(); j++) {
            if (nums[i] + nums[j] == target) {
                return {i, j};  // 找到了！
            }
        }
    }
    return {};  // 没找到
}
```
**特点**: 简单直接，但比较慢 🐌

**解法2: 哈希表 - 用"电话本"找朋友**
```cpp
vector<int> twoSum(vector<int>& nums, int target) {
    unordered_map<int, int> map;  // 数字 -> 位置的"电话本"
    for (int i = 0; i < nums.size(); i++) {
        int complement = target - nums[i];  // 要找的"朋友"
        if (map.find(complement) != map.end()) {
            return {map[complement], i};  // 找到了！
        }
        map[nums[i]] = i;  // 记录这个数字的位置
    }
    return {};  // 没找到
}
```
**特点**: 很快，但需要额外空间 🚀

### 例题2: 三数之和 - 三人行游戏 👨‍👩‍👧
**题目**: 给定一个包含n个整数的数组，找出所有三个数的组合，使得它们的和等于0。

**生活例子**: 就像找三个朋友，他们三个人的年龄加起来等于某个数！

```cpp
vector<vector<int>> threeSum(vector<int>& nums) {
    vector<vector<int>> result;
    sort(nums.begin(), nums.end());  // 先排序，方便找朋友
    
    for (int i = 0; i < nums.size() - 2; i++) {
        if (i > 0 && nums[i] == nums[i-1]) continue; // 跳过重复的
        
        int left = i + 1, right = nums.size() - 1;
        while (left < right) {
            int sum = nums[i] + nums[left] + nums[right];
            if (sum == 0) {  // 找到了！
                result.push_back({nums[i], nums[left], nums[right]});
                
                // 跳过重复的朋友
                while (left < right && nums[left] == nums[left+1]) left++;
                while (left < right && nums[right] == nums[right-1]) right--;
                
                left++;
                right--;
            } else if (sum < 0) {
                left++;  // 和太小，左边往右移
            } else {
                right--; // 和太大，右边往左移
            }
        }
    }
    
    return result;
}
```

### 例题3: 最长递增子序列 - 排队游戏 📏
**题目**: 给定一个无序的整数数组，找到其中最长严格递增子序列的长度。

**生活例子**: 就像排队，要找到最长的"从矮到高"的队伍！

**解法1: 动态规划 - 记录每个位置的最长队伍**
```cpp
int lengthOfLIS(vector<int>& nums) {
    if (nums.empty()) return 0;
    
    vector<int> dp(nums.size(), 1);  // 每个位置至少能排1个人
    int maxLength = 1;
    
    for (int i = 1; i < nums.size(); i++) {
        for (int j = 0; j < i; j++) {
            if (nums[j] < nums[i]) {  // 前面的人比我矮
                dp[i] = max(dp[i], dp[j] + 1);  // 我可以排在他后面
            }
        }
        maxLength = max(maxLength, dp[i]);  // 更新最长队伍
    }
    
    return maxLength;
}
```

**解法2: 二分查找优化 - 维护最优队伍**
```cpp
int lengthOfLIS(vector<int>& nums) {
    vector<int> tails;  // 记录每个长度队伍的最后一个人
    
    for (int num : nums) {
        auto it = lower_bound(tails.begin(), tails.end(), num);
        if (it == tails.end()) {
            tails.push_back(num);  // 开始新的队伍
        } else {
            *it = num;  // 替换更优的人
        }
    }
    
    return tails.size();  // 最长队伍的长度
}
```

**例题总结**:
- 两数之和：找朋友，用"电话本"更快 👫
- 三数之和：三人行，排序后双指针 👨‍👩‍👧  
- 最长递增：排队游戏，动态规划或二分查找 📏

---

## 8. 挑战练习 🏆

### 基础练习 - 新手村任务 🎯
1. **排序练习 - 整理房间大师**
   - 用冒泡排序整理你的书桌
   - 用选择排序整理你的衣柜
   - 用插入排序整理你的书包
   - 比较哪种方法最快！

2. **查找练习 - 侦探游戏**
   - 在电话本里找朋友（线性查找）
   - 在字典里找单词（二分查找）
   - 在旋转的转盘上找数字
   - 找山峰（峰值元素）

3. **贪心练习 - 做选择高手**
   - 跳跃游戏：能跳到终点吗？
   - 加油站问题：能绕一圈吗？
   - 分发糖果：让每个孩子都开心

### 进阶练习 - 冒险者任务 ⚔️
1. **递归与分治 - 魔法师技能**
   - 快速计算幂次（快速幂）
   - 合并多个有序队伍
   - 计算数组中的逆序对

2. **模拟算法 - 工程师任务**
   - 旋转图像（转魔方）
   - 画螺旋矩阵（画螺旋线）
   - 生命游戏（细胞繁殖）

3. **综合应用 - 终极挑战**
   - 最长公共子序列（找共同朋友）
   - 编辑距离（改错别字）
   - 最大矩形面积（找最大房间）

### 竞赛真题 - 王者挑战 👑
1. **CSP-J真题 - 初中生专属**
   - 2023年：小苹果（贪心+模拟）
   - 2022年：上升点列（动态规划）
   - 2021年：网络连接（字符串处理）

2. **NOIP真题 - 高中生挑战**
   - 2019年：数字游戏（数学+模拟）
   - 2018年：货币系统（贪心+数学）
   - 2017年：逛公园（图论+动态规划）

---

## 学习建议 - 成为算法高手 🎓

### 1. 理论学习 - 打好基础 📚
- **理解原理**: 不要死记硬背，要理解为什么这样做
- **分析复杂度**: 知道哪个算法快，哪个算法慢
- **适用场景**: 知道什么时候用什么算法

### 2. 实践练习 - 多写代码 💻
- **从简单开始**: 先做简单的题目，再挑战难的
- **多写多练**: 熟能生巧，代码写多了就熟练了
- **注意质量**: 代码要清晰易懂，不要乱写

### 3. 总结反思 - 不断进步 📝
- **记录心得**: 把学到的知识记下来
- **分析错题**: 做错的题目要仔细分析
- **建立体系**: 把学到的知识整理成体系

### 4. 竞赛准备 - 冲击高分 🏆
- **熟悉环境**: 了解竞赛的规则和环境
- **掌握模板**: 记住常用的代码模板
- **培养习惯**: 养成良好的编程习惯

---

## 课后作业 - 巩固练习 📋

### 必做作业（基础）
1. **排序算法实践**
   - 实现冒泡、选择、插入、快速、归并排序
   - 比较它们在不同数据下的性能
   - 处理重复元素和逆序数据

2. **查找算法练习**
   - 实现线性查找和二分查找
   - 解决旋转数组查找问题
   - 实现查找第一个/最后一个位置

3. **贪心算法应用**
   - 解决活动选择问题
   - 实现找零钱算法
   - 完成跳跃游戏

### 选做作业（进阶）
4. **递归与分治**
   - 实现斐波那契数列的三种方法
   - 解决汉诺塔问题
   - 实现快速幂算法

5. **模拟算法**
   - 实现螺旋矩阵生成
   - 解决约瑟夫环问题
   - 实现图像旋转

### 挑战作业（高手）
6. **综合应用**
   - 解决两数之和、三数之和问题
   - 实现最长递增子序列
   - 挑战竞赛真题

---

## 学习进度检查 ✅

### 第一周目标
- [ ] 掌握所有排序算法
- [ ] 理解查找算法原理
- [ ] 完成基础练习题

### 第二周目标
- [ ] 掌握贪心算法思想
- [ ] 理解递归与分治
- [ ] 完成进阶练习题

### 第三周目标
- [ ] 掌握模拟算法
- [ ] 完成综合应用题
- [ ] 挑战竞赛真题

---

## 学习资源推荐 📖

### 在线练习平台
- **洛谷**: 适合初学者的中文平台
- **Codeforces**: 国际知名竞赛平台
- **AtCoder**: 日本竞赛平台，题目质量高

### 学习网站
- **算法导论**: 经典算法教材
- **LeetCode**: 面试和竞赛题目
- **牛客网**: 中文算法学习平台

### 推荐书籍
- 《算法竞赛入门经典》
- 《算法竞赛进阶指南》
- 《数据结构与算法分析》

---

**记住**: 算法学习就像玩游戏，需要不断练习和挑战！每个算法都有自己的"技能点"，掌握了就能在编程的世界里游刃有余！加油，未来的算法大师！🚀✨
